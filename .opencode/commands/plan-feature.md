# Plan Feature

Use this command when planning a new UMBRAL feature.

Required steps:

1. Use `umbral-context`.
2. Use `sdd-workflow`.
3. Identify the user story.
4. Identify the owning microservice.
5. Read the related SDD files.
6. Produce a plan before coding.
