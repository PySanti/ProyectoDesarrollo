# Current Traceability Matrix

## Rule

Traceability rows must reference the current source documents, target service ownership, current contracts, tests, and acceptance evidence.

| Feature | Requirement | Owning service | Supporting services | SDD folder | Contracts | Status |
|---|---|---|---|---|---|---|
| Partida/Juego model + Partidas configuration (SP-2) | Create a Partida header and add fully-formed Juegos (Trivia `Pregunta`/`Opcion`, BDT `EtapaBDT`) in sequential order; review queries (detail + list); `EstadoPartida` nullable until publish (SP-3 SEAM) | Partidas | — (SP-3 runtime / SP-4 scoring consume downstream) | docs/superpowers/specs/2026-06-24-sp2-partida-juego-model-partidas-config-design.md · docs/superpowers/plans/2026-06-24-sp2-partida-juego-model-partidas-config.md | contracts/http/partidas-config.md | Implemented — 102/102 green (91 unit + 5 integration + 6 contract); R1 structural gate passed; whole-branch review READY TO INTEGRATE; doctrine-conformance audit (6-dim multi-agent) = **CompliantWithDeviations** (0 Critical / 0 Important / 7 Minor + 1 disputed; ver `.superpowers/sdd/sp2-doctrine-conformance-report.md`). **7 Minor resueltos** (M-1..M-7, commits `038cab1..fa66451`): vocab SP-3 neutralizado, validación movida a `ValidationBehavior` (pipeline MediatR), `HealthControllerTests` reubicado en `Api/`, y cubiertos los huecos 409/Location/orden<1/colisión-BDT. M-8 (clasificación DDD de `Opcion`) dejado como **deuda documentada** (existencia de `OpcionId` plan-mandated por contrato). Suite **106/106 verde** (94 unit + 5 integration + 7 contract). |
| Publicación → Lobby + inscripciones Individual (SP-3a) | Publicar una `Partida` configurada a `Lobby` (snapshot de config por HTTP interno `GET /partidas/{id}`, Opción A); inscripción **Individual** con invariante "una participación activa a la vez"; cancelar inscripción propia; query de lobby. Estado runtime en `SesionPartida.EstadoSesion` (R1, ADR-0010); eventos de dominio por port No-Op | Operaciones de Sesión | Partidas (snapshot de config vía HTTP, solo lectura) | docs/superpowers/specs/2026-06-26-sp3a-publicacion-lobby-inscripcion-design.md · docs/superpowers/plans/2026-06-26-sp3a-publicacion-lobby-inscripcion.md | contracts/http/operaciones-sesion-api.md · contracts/events/operaciones-sesion-events.md | Implemented — **69/69 verde** (59 unit + 3 integration + 7 contract); R1 structural gate passed; ADR-0010 aceptado (estado runtime en Operaciones, `EstadoPartida` de Partidas queda `null`). Handoff config = HTTP síncrono (404→null vs 502 Partidas-inaccesible); eventos por `NoOpSesionEventsPublisher` (backbone RabbitMQ diferido a slice propio antes de SP-4). **Diferido:** Equipo/convocatorias→SP-3a-E, inicio/secuenciación→SP-3b, runtime Trivia/BDT→SP-3c/3d, reconexión→SP-3e, SignalR→SP-3f. Auditoría de conformidad (12-dim, 4 auditores subagent) 2026-06-27 = **CONFORME** (0 Critical / 0 Important / 0 Minor); suite re-corrida en HEAD 111/111 verde; ver `docs/04-sdd/auditorias/2026-06-27-informe-conformidad-sp3ab.md`. |
| Inicio (manual/automático) + secuenciación de juegos (SP-3b) | Iniciar una `SesionPartida` publicada (manual + automático idempotente time-gated), chequeo de mínimos → auto-cancelación, activación secuencial de juegos (`EstadoJuego` Pendiente→Activo→Finalizado) hasta `Terminada`; eventos por puerto No-Op | Operaciones de Sesión | — (Puntuaciones consume eventos en SP-4) | docs/superpowers/specs/2026-06-26-sp3b-inicio-secuenciacion-design.md · docs/superpowers/plans/2026-06-26-sp3b-inicio-secuenciacion.md | contracts/http/operaciones-sesion-api.md · contracts/events/operaciones-sesion-events.md | Implemented — full suite verde; R1 gate passed; eventos PartidaIniciada/JuegoActivado/PartidaFinalizada/PartidaCancelada (No-Op). **Diferido:** gameplay Trivia/BDT→3c/3d (disparo real de FinalizarJuegoActual), scheduler del automático→3f, reconexión→3e, SignalR→3f, Equipo→3a-E. Auditoría de conformidad (12-dim, 4 auditores subagent) 2026-06-27 = **CONFORME** (0 Critical / 0 Important / 0 Minor); suite re-corrida en HEAD 111/111 verde; watch-item concurrencia trazado a SP-3f; ver `docs/04-sdd/auditorias/2026-06-27-informe-conformidad-sp3ab.md`. |
| Runtime Trivia Individual (SP-3c) | Validar respuestas en vivo de un JuegoTrivia activo (primera-correcta cierra; ventana de tiempo), avance operador-dirigido de preguntas, agotar→FinalizarJuegoActual; snapshot de preguntas al publicar; eventos por puerto No-Op | Operaciones de Sesión | Partidas (snapshot de preguntas vía HTTP, solo lectura); Puntuaciones consume eventos en SP-4 | docs/superpowers/specs/2026-06-27-sp3c-runtime-trivia-design.md · docs/superpowers/plans/2026-06-27-sp3c-runtime-trivia.md | contracts/http/operaciones-sesion-api.md · contracts/events/operaciones-sesion-events.md | Implemented — suite verde. **Diferido:** Equipo Trivia→SP-3a-E, barrido automático de timeout + SignalR→SP-3f, runtime BDT→SP-3d, scoring/ranking real→SP-4. |
| Runtime BDT Individual (SP-3d) | Validar tesoros QR en vivo de un JuegoBDT activo (decode backend vs CodigoQREsperado; 1ra validación correcta o timeout cierra la etapa; ventana de tiempo), registro de cada intento (TesoroQR), avance secuencial automático de etapas, agotar→FinalizarJuegoActual; snapshot de etapas al publicar; eventos por puerto No-Op | Operaciones de Sesión | Partidas (snapshot de etapas vía HTTP, solo lectura); Puntuaciones consume eventos en SP-4 | docs/superpowers/specs/2026-06-28-sp3d-runtime-bdt-design.md · docs/superpowers/plans/2026-06-28-sp3d-runtime-bdt.md | contracts/http/operaciones-sesion-api.md · contracts/events/operaciones-sesion-events.md | Implemented — suite verde. **Diferido:** Equipo BDT→slice-E, pistas + geolocalización + barrido automático de timeout + SignalR→SP-3f, scoring/ranking real→SP-4, reconexión→SP-3e. |
| Reconexión / estado vigente (SP-3e) | Agregador GET /mi-sesion (por participante, JWT sub): snapshot único de participación + estado partida + juego activo + sub-estado vigente (pregunta/etapa activa, DTOs participant-safe reusados) + yaRespondioPreguntaActual; read-only CQRS, sin eventos; cubre Lobby+Iniciada, terminal/sin-participación → 204; Individual-only | Operaciones de Sesión | — (Puntuaciones SP-4; push SignalR SP-3f) | docs/superpowers/specs/2026-06-29-sp3e-reconexion-design.md · docs/superpowers/plans/2026-06-29-sp3e-reconexion.md | contracts/http/operaciones-sesion-api.md | Implemented — suite verde. **Fuente:** RF-14/RB-33. **Diferido:** Equipo/convocatoria→slice-E, push tiempo real→SP-3f, scoring→SP-4. |
| Concurrencia + barridos por tiempo (SP-3f-1) | Token optimista `xmin` en SesionPartida (condicional Npgsql) + un BackgroundService (MantenimientoSesionesWorker) que por tick dispara BarrerIniciosAutomaticos (Lobby+Automatico/ManualYAutomatico, now≥TiempoInicio → inicia/auto-cancela por mínimos) y BarrerTimeouts (Iniciada con pregunta/etapa vencida → CerrarActividadVencida: cierra por tiempo, avanza, finaliza si último). Reúsa dominio existente; conflicto en request → 409; eventos No-Op; Individual-only | Operaciones de Sesión | — (Puntuaciones SP-4; push SignalR SP-3f-2) | docs/superpowers/specs/2026-06-29-sp3f1-concurrencia-barridos-design.md · docs/superpowers/plans/2026-06-29-sp3f1-concurrencia-barridos.md | contracts/http/operaciones-sesion-api.md | Implemented — suite 195u·16i·32c=243 verde. **Fuente:** auto-inicio diferido SP-3b, timeout diferido SP-3c/3d, watch-item del token de concurrencia. **Diferido:** Equipo→slice-E, push tiempo real→SP-3f-2, RabbitMQ real→slice propio, scoring→SP-4, enforcement xmin a nivel-DB no integration-testeado (InMemory ignora tokens; contrato de comportamiento sí cubierto: handler salta en DbUpdateConcurrencyException + middleware→409), aislamiento de conflicto por-candidato dentro del tick (hoy por-tick auto-sanado: barridos comparten 1 DbContext scoped; sin corrupción ni doble-publish, próximo tick reevalúa — review final I1). **Review final (opus):** C1 corregido (scan auto-inicio incluye Preguntas/Etapas; sin él el juego quedaba Activo sin paso en Npgsql); I1 aceptado+documentado. |
| Push tiempo real SignalR (SP-3f-2) | Hub SignalR (SesionHub) en Operaciones de Sesión que difunde por grupo `partida:{id}` las transiciones runtime (lobby/iniciada/juego/pregunta/etapa activada+cerrada+ganada/cancelada/finalizada) colgándose del puerto ISesionEventsPublisher vía composite NoOp+SignalR (cero churn de handlers); deadline-timestamp para timers; auth WS por access_token query (servicio+gateway); payloads delgados anti-leak | Operaciones de Sesión | Gateway (passthrough WS + JWT query) | docs/superpowers/specs/2026-06-30-sp3f2-push-tiempo-real-signalr-design.md · docs/superpowers/plans/2026-06-30-sp3f2-push-tiempo-real-signalr.md | contracts/http/operaciones-sesion-api.md | Implemented — suite verde. **Diferido:** ranking en vivo→Puntuaciones SP-4, cableado clientes web/móvil→follow-up, broker RabbitMQ real→slice propio (se sumará como otra impl del composite), targeting por participante, pistas+geolocalización→sub-slices SP-3f, Equipo→slice-E. **Gap documentado:** camino WS-a-través-de-YARP no integration-testeado (verificado por unit+contract); desajuste de prefijo gateway↔operaciones es deuda pre-existente que afecta a todos los endpoints por igual. |
| Alineación de ruteo Operaciones ↔ gateway (SP-3g) | Operaciones adopta la convención de Partidas: SesionesController `[Route("operaciones-sesion")]` + hub `operaciones-sesion/hubs/sesion` + guard JWT del servicio prefijado; gateway sin cambios (reenvío-puro, sin PathRemovePrefix). Resuelve el Important del review de SP-3f-2 (hub/endpoints inalcanzables vía gateway). Tests e2e a nivel-servicio en el path público prefijado (Rutas.Base) + test negotiate del hub bajo el prefijo | Operaciones de Sesión | Gateway (reenvío-puro, sin cambios) | docs/superpowers/specs/2026-06-30-sp3g-alineacion-ruteo-gateway-design.md · docs/superpowers/plans/2026-06-30-sp3g-alineacion-ruteo-gateway.md | contracts/http/operaciones-sesion-api.md | Implemented — suite verde. **Gap documentado:** e2e real gateway→servicio-vivo no testeable en el harness (destinos no corren en test); cubierto por contract tests a nivel-servicio en el path público. **Forward-looking:** identity/puntuaciones deben usar `[Route("<servicio>")]` al ganar controllers de dominio. |
| Geolocalización BDT (SP-3f-3) | Canal SignalR de ubicación sobre SesionHub: método client→servidor EnviarUbicacion(latitud, longitud) (solo participante, partida tomada de la conexión, rango validado) reenvía UbicacionParticipantePayload solo al grupo operador-scoped `operador:partida:{id}` (BR-B07: solo el operador ve el mapa); identidad desde Context.Items poblado en SuscribirAPartida (sin lectura DB por tick → RNF-15); timestamp server-stamped (TimeProvider). Relay puro: sin persistencia, sin evento, sin cambio de dominio | Operaciones de Sesión | Gateway (passthrough WS); clientes web/móvil (cableado en follow-up) | docs/superpowers/specs/2026-07-01-sp3f3-geolocalizacion-bdt-design.md · docs/superpowers/plans/2026-07-01-sp3f3-geolocalizacion-bdt.md | contracts/http/operaciones-sesion-api.md | Implemented — suite verde. **Diferido:** persistencia/last-known + audit de ubicación (TipoEventoHistorial.Ubicacion)→broker RabbitMQ, GeolocalizacionAutorizada backend→modelo participación BDT, gate server-side de tipo-de-juego por tick (contra RNF-15), cableado clientes→follow-up, Equipo→slice-E. **Gap documentado:** relay WS end-to-end no testeable en el harness (cubierto por unit tests del hub). |
| Pistas BDT (SP-3f-4) | Canal SignalR operador→participante de pistas sobre SesionHub: comando `POST /operaciones-sesion/partidas/{partidaId}/pistas` (HU/BR-B06) dispara `EnviarPistaCommand` → dominio read-only `PrepararPista(Guid participanteDestinoId)` en `SesionPartida` (reusa 3 excepciones existentes: sesión no existe, no iniciada, destino no inscrito) → publica `PistaEnviadaEvent` vía `ISesionEventsPublisher` (event-only, sin persistencia ni migración) → `SignalRSesionEventsPublisher` empuja el mensaje `PistaEnviada` SOLO al grupo por-participante `participante:{destinoId}` (join automático en `SuscribirAPartida` rama participante; `DesuscribirDePartida` lo retira). El `texto` viaja en el payload — única excepción documentada al anti-leak porque es contenido dirigido exclusivamente a su receptor. Sin replay (destino offline → pista perdida, transitorio) | Operaciones de Sesión | Gateway (passthrough WS); clientes web/móvil (cableado en follow-up) | docs/superpowers/specs/2026-07-01-sp3f4-pistas-bdt-design.md · docs/superpowers/plans/2026-07-01-sp3f4-pistas-bdt.md | contracts/http/operaciones-sesion-api.md | Implemented — suite verde (47/47 contract). **Diferido:** persistencia de la entidad `Pista` + audit (BR-B06) →broker RabbitMQ, replay/last-known para destino offline, cableado clientes web/móvil→follow-up, Equipo→slice-E, hardening adicional de rol operador. |
| Participación por Equipo — preinscripción + convocatorias (SP-3e-1) | Habilita la modalidad **Equipo** a nivel participación en Operaciones de Sesión: el líder preinscribe su equipo activo (`POST /operaciones-sesion/partidas/{partidaId}/inscripciones-equipo`, snapshot HTTP síncrono a Identity `GET /api/teams/mine` vía `IEquipoDirectoryClient`/`IdentityEquipoHttpClient`, congelado al preinscribir) generando una `Convocatoria` (entidad hija de `InscripcionPartida`) por integrante; cada convocado acepta/rechaza (`POST /operaciones-sesion/convocatorias/{convocatoriaId}/aceptacion`\|`/rechazo`); el líder puede cancelar la preinscripción (`DELETE .../inscripciones-equipo/mia`). Dominio: `SesionPartida.PreinscribirEquipo`/`ResponderConvocatoria`/`CancelarInscripcionEquipo`; participante-activo (Equipo) = convocatoria **Aceptada** en inscripción activa (extiende `ParticipanteTieneParticipacionActivaAsync`, BR-G09); cupo (`Maximos`) = inscripciones de equipo activas; mínimos = equipos con ≥1 aceptado (mismo patrón de auto-cancelación que Individual, sin two-phase `Confirmar/ExcluirPorMinimos`). Eventos `ConvocatoriaCreada`/`ConvocatoriaRespondida` por el seam `ISesionEventsPublisher` (5 implementadores); SignalR empuja `ConvocatoriaCreada` SOLO al grupo por-participante `participante:{convocado}` (reusa el grupo de SP-3f-4); `ConvocatoriaRespondida` sin push en este slice. Proyecciones: `LobbyDto.Equipos[]{ equipoId, convocados, aceptados }`, `MiSesionDto.Convocatoria?{ convocatoriaId, equipoId, estado }` | Operaciones de Sesión | Identity (snapshot de membresía vía HTTP `GET /api/teams/mine`, solo lectura, SP-3e-1a) | docs/superpowers/specs/2026-07-01-sp3e1-participacion-equipo-design.md · docs/superpowers/plans/2026-07-01-sp3e1b-operaciones-participacion-equipo.md | contracts/http/operaciones-sesion-api.md | Implemented — suite verde (285 unit + 22 integration + 48 contract). **Fuente:** BR-G09 (una participación activa a la vez, extendida a convocatoria aceptada). **Diferido:** runtime Trivia/BDT/pistas en modalidad Equipo→SP-3e-2/3/4, scoring/ranking→SP-4, broker RabbitMQ real para `ConvocatoriaCreada`/`ConvocatoriaRespondida`→slice propio, cableado clientes web/móvil→follow-up. |
| Runtime Trivia Equipo — identidad dual + guard BR-G09 + inbox convocatorias (SP-3e-2) | Habilita responder Trivia en modalidad **Equipo** con identidad dual (autor real en `ParticipanteId`, equipo en `EquipoId` nuevo `Guid?`, null⇔Individual): en `SesionPartida.ResponderPregunta`, cuando `Modalidad == Equipo` resuelve el `equipoId` por convocatoria **Aceptada** del caller en inscripción activa (403 `ParticipanteNoInscritoException` si no hay) y `PreguntaSnapshot.RegistrarRespuesta` sella **una respuesta por equipo** (primera respuesta de cualquier miembro, correcta o no; duplicada→409 `RespuestaDuplicadaException`); ganador de pregunta puede ser `GanadorEquipoId`. Cierra el hueco BR-G09 intra-partida: `ResponderConvocatoria` rechaza (409 `ParticipacionActivaExistenteException`) aceptar una segunda convocatoria de la misma sesión ya con una Aceptada. Eventos `RespuestaTriviaValidadaEvent`/`PuntajeTriviaIncrementadoEvent`/`PreguntaTriviaCerradaEvent` extendidos con `EquipoId?`/`GanadorEquipoId?` (default null, no rompe construcción existente); columnas EF nuevas `EquipoId`/`GanadorEquipoId` + migración. `MiSesionDto.yaRespondioPreguntaActual` en Equipo pasa a significar "mi equipo ya respondió" (`Respuestas.Any(EquipoId == miEquipo)`). Inbox nuevo de solo lectura: `GET /operaciones-sesion/mis-convocatorias` (`ObtenerMisConvocatoriasPendientesQuery` → repo scan de convocatorias Pendientes con partida en Lobby e inscripción de equipo activa, ordenadas por `fechaEnvio`) → `ConvocatoriaPendienteDto[]`. Individual no cambia de comportamiento (todo campo nuevo `Guid?` default null) | Operaciones de Sesión | — (Puntuaciones consume eventos extendidos en SP-4) | docs/superpowers/specs/2026-07-01-sp3e2-runtime-trivia-equipo-design.md · docs/superpowers/plans/2026-07-01-sp3e2-runtime-trivia-equipo.md | contracts/http/operaciones-sesion-api.md | Implemented — suite verde (300 unit + 27 integration + 48 contract). **Fuente:** BR-G09 (guard intra-partida), RF-Trivia-Equipo (1 respuesta por equipo). **Diferido:** runtime BDT Equipo/pistas Equipo→slice propio, scoring/ranking real para equipos→SP-4, broker RabbitMQ real para eventos extendidos→slice propio, cableado clientes web/móvil (inbox de convocatorias, respuesta Trivia Equipo)→follow-up. |
| Runtime BDT Equipo — identidad dual sobre ValidarTesoro/TesoroQR, reintentos libres, ganador equipo (SP-3e-3) | Habilita subir/validar QR de tesoro en modalidad **Equipo**, espejo del patrón identidad dual de SP-3e-2 pero **sin dedup** (a diferencia de Trivia): en `SesionPartida.ValidarTesoro`, cuando `Modalidad == Equipo` resuelve el `equipoId` por convocatoria **Aceptada** del caller en inscripción activa (403 `ParticipanteNoInscritoException` si no hay, mismo guard que Trivia); `EtapaSnapshot.RegistrarTesoro` **no sella nada en intento fallido** — reintentos ilimitados, cualquier miembro puede reintentar hasta cierre de etapa; la primera validación correcta dentro de la ventana marca la etapa `Ganada` con `GanadorParticipanteId` = autor y nuevo `GanadorEquipoId` = su equipo. `TesoroQR` += `EquipoId?` (autor + equipo en cada intento, correcto o no); `ResultadoRegistroTesoro` += `EquipoId?`/`GanadorEquipoId?` (trailing, default null). Eventos `TesoroQRValidadoEvent`/`EtapaBDTGanadaEvent`/`EtapaBDTCerradaEvent` extendidos con `EquipoId?`/`GanadorEquipoId?` (default null, no rompe construcción existente); columnas EF nuevas `equipoid`/`ganadorequipoid` + migración `SP3e3RuntimeBdtEquipo` (additiva). Sin cambios en payloads SignalR (`EtapaActivada`/`EtapaCerrada`/`EtapaGanada` no portan identidad) ni en proyecciones (BDT no tiene equivalente de `yaRespondioPreguntaActual`); sin endpoints nuevos. Individual no cambia de comportamiento (todo campo nuevo `Guid?` default null) | Operaciones de Sesión | — (Puntuaciones consume eventos extendidos en SP-4) | docs/superpowers/specs/2026-07-02-sp3e3-runtime-bdt-equipo-design.md · docs/superpowers/plans/2026-07-02-sp3e3-runtime-bdt-equipo.md | contracts/http/operaciones-sesion-api.md | Implemented — suite verde (308 unit + 28 integration + 48 contract). **Fuente:** SRS BDT (ganador de etapa por primera validación correcta de cualquier miembro; sin regla de intento único, a diferencia de Trivia). **Diferido:** pistas Equipo→SP-3e-4, scoring/ranking real para equipos→SP-4, broker RabbitMQ real para eventos extendidos→slice propio, cableado clientes web/móvil→follow-up. |
| Pistas BDT Equipo — destino dual (participante/equipo), grupo SignalR `equipo:{id}` (SP-3e-4) | Habilita enviar una pista con destino **equipo entero** en modalidad Equipo (todos los miembros conectados la reciben), preservando el destino a un participante puntual sin cambios. Dominio nuevo `SesionPartida.PrepararPistaEquipo(equipoDestinoId)`: 409 `ModalidadNoSoportadaException` si la partida no es Equipo; 404 `InscripcionNoEncontradaException.ParaEquipo` si el equipo no tiene inscripción activa; mismos guards BDT que `PrepararPista` (`JuegoActivoNoEsBDTException`/`NoHayEtapaActivaException`). Cadena dual-destino: `EnviarPistaRequest`/`EnviarPistaCommand`/`PistaEnviadaEvent`/`PistaEnviadaResponse`/`PistaEnviadaPayload` ganan `Guid? EquipoDestinoId` trailing (default null) y `ParticipanteDestinoId` pasa a `Guid?`; `EnviarPistaCommandValidator` exige **exactamente un destino** (XOR, 400 si no); `EnviarPistaCommandHandler` ramifica por destino (`EquipoDestinoId` presente → `PrepararPistaEquipo`, si no → `PrepararPista`, mismo 403 `ParticipanteNoInscritoException` de antes para destino-participante). `SignalRSesionEventsPublisher.PublicarPistaEnviadaAsync` rutea al grupo `equipo:{equipoDestinoId}` si viene, si no al grupo `participante:{id}` existente. `SesionHub.SuscribirAPartida` (rama participante), en sesiones Equipo, además une la conexión al grupo `equipo:{equipoId}` cuando el caller tiene convocatoria Aceptada en su inscripción activa (resuelto server-side por el `sub` del JWT contra la sesión ya cargada); `DesuscribirDePartida` la retira. Event-only, sin persistencia ni migración EF (mismo patrón SP-3f-4). Individual no cambia de comportamiento (todo campo nuevo `Guid?` default null) | Operaciones de Sesión | — (Puntuaciones consume eventos extendidos en SP-4) | docs/superpowers/specs/2026-07-02-sp3e4-pistas-equipo-design.md · docs/superpowers/plans/2026-07-02-sp3e4-pistas-equipo.md | contracts/http/operaciones-sesion-api.md | Implemented — suite verde (323 unit + 28 integration + 48 contract). **Fuente:** BR-B06 (la pista es para un destino específico, participante o equipo entero — nunca ambos ni el resto de la partida). **Diferido:** scoring/ranking real para equipos→SP-4, broker RabbitMQ real para eventos extendidos→slice propio, cableado clientes web/móvil (envío de pista a equipo desde web, recepción en móvil)→follow-up. |
| Remediación auditoría — Contrato de eventos + controller test + deadline Trivia + convocatoria Aceptada + desambiguación SP-3e (SP-3h) | Cierre documental + cobertura de la auditoría SP-3c→3e-4: (H1) Contrato de eventos alineado a slice-E (6 samples RespuestaTriviaValidada/PuntajeTriviaIncrementado/PreguntaTriviaCerrada/TesoroQRValidado/EtapaBDTGanada/EtapaBDTCerrada con `equipoId?`/`ganadorEquipoId?` + 3 eventos seam PistaEnviada/ConvocatoriaCreada/ConvocatoriaRespondida registrados en la tabla canónica) + Status HTTP; (H2) ADR-0011 sanciona membresía de grupos vía repositorio en hubs (`SesionHub.SuscribirAPartida` → `ISesionPartidaRepository` directo en Api, handshake realtime no-command) + desambiguación SP-3e vs SP-3e-1..4 en notas; (H3) Borde de deadline Trivia inclusivo (`>=` unificado) + `ParaEquipo` en cancelación de inscripción; (H4) Mi-sesión prefiere convocatoria Aceptada (resolver equipoId en `ResponderConvocatoria` dual cuando hay ya una aceptada); (H5) Controller test `ObtenerEtapaActual` (21/21 cobertura) + estilo `.Group`. Rango: c6ac2a5..9295091 (5 commits). **Estado post-remediación: CONFORME — 0 Critical · 0 Important · 0 Minor.** Suites en HEAD: Unit 327 / Integration 28 / Contract 48 | Operaciones de Sesión | — | docs/superpowers/specs/2026-07-03-sp3h-remediacion-auditoria-design.md · docs/superpowers/plans/2026-07-03-sp3h-remediacion-auditoria.md | contracts/http/operaciones-sesion-api.md · contracts/events/operaciones-sesion-events.md | Implemented — suite verde (327 unit + 28 integration + 48 contract). **Fuente:** Informe de auditoría 2026-07-02 (0 Critical / 4 Important / 3 Minor → CONFORME post-remediación). **Alcance:** I-1..I-4 + m-1..m-3 + 3 minors ledger. **Commits:** c6ac2a5 (I-1, I-2, m-2) · 6baab6c (I-4, m-3) · a64132d (m-1 + ledger) · ed89296 (ledger mi-sesión) · 9295091 (I-3 + ledger). |
| Backbone RabbitMQ — publisher dual-write + eventos reales (SP-3i) | Reemplaza el transporte No-Op de `ISesionEventsPublisher` por RabbitMQ real vía Composite (dual-write junto a SignalR, cero churn de handlers): transporte base (`RabbitMqOptions`/`RabbitMqPublishChannel`/`EventEnvelope` camelCase `{eventId,eventType,version,occurredAt,payload}`) sobre exchange topic durable `umbral.operaciones-sesion` con mapa explícito de routing keys `operaciones-sesion.<evento-kebab>.v1` (`SesionEventRouting`, sin kebab-case algorítmico); `RabbitMqSesionEventsPublisher` best-effort estricto (try/catch + `LogError`, nunca lanza al caller, registro condicional en el Composite); `UbicacionActualizadaEvent` (seam 17) publicado al broker desde `SesionHub`; re-push SignalR de convocatorias pendientes en `OnConnectedAsync`; consumidor de humo en Puntuaciones (cola + log, envelope propio duplicado por doctrina de límites de servicio); integration test opt-in de round-trip real (`RabbitMqRoundTripTests`, sin `RABBITMQ_TEST_HOST` retorna vacío) | Operaciones de Sesión | Puntuaciones (consumidor de humo — cola + log, sin lógica de negocio) | docs/superpowers/specs/2026-07-03-sp3i-backbone-rabbitmq-design.md · docs/superpowers/plans/2026-07-03-sp3i-backbone-rabbitmq.md | contracts/events/operaciones-sesion-events.md | Implemented — suite verde en Operaciones (356 unit + 29 integration + 48 contract, el nuevo integration test opt-in retorna vacío sin broker); Puntuaciones (7 unit + 2 integration + 1 contract). Humo real verificado con broker vivo (`docker compose up -d rabbitmq` + `RABBITMQ_TEST_HOST=localhost --filter RabbitMqRoundTripTests`) = **PASS** (1/1). **Fuente:** ADR-0012 (publicación best-effort post-save; outbox transaccional diferido, criterio de activación documentado). **Alcance:** backbone RabbitMQ: publisher dual-write + ubicación + re-push convocatorias + humo Puntuaciones. **Commits:** 6e3bc10..d13f059 (+ cierre) — 6e3bc10 (B1 transporte base: options/envelope/routing) · 528dfd3 (B2 publisher best-effort + DI condicional en Composite) · 5de747c (B3 UbicacionActualizada al broker, seam 17 + hub) · f8710b1 + 07c9011 (B4 re-push SignalR de convocatorias pendientes en OnConnectedAsync) · 0ca32eb + d13f059 (B5 consumidor de humo RabbitMQ en Puntuaciones + EnvelopeReader nunca lanza) · B6 (este cierre: integration test opt-in + ADR-0012 + traceability). **Diferido:** outbox transaccional (ver criterio de activación en ADR-0012), dual-write end-to-end no cubierto en CI por defecto (integration test opt-in requiere broker vivo vía `RABBITMQ_TEST_HOST`), cableado de consumidores reales de negocio en Puntuaciones (hoy solo humo cola+log)→SP-4. |
| Puntuaciones — consumidor real de proyecciones + rankings nativos (SP-4a) | Consumidor RabbitMQ real (cola `puntuaciones.operaciones-sesion.proyecciones`, 7 bindings, dedup por `eventId` transaccional, ack-siempre best-effort ADR-0012, cola de humo SP-3i eliminada); proyecciones `partidas_proyectadas`/`juegos_proyectados`/`marcadores`/`eventos_procesados` con identidad dual (`CompetidorId = equipoId ?? participanteId`) y tolerancia al desorden (stubs + estado monotónico + acumulación conmutativa); ranking nativo on-read (puntos DESC, tiempo ASC, empate exacto comparte posición; `unidadesGanadas` informativo — doctrina BDT por puntos) expuesto en `GET .../ranking` y `GET .../marcadores/{competidorId}` (404 vía middleware). | Puntuaciones | Operaciones de Sesión (productor de los 7 eventos consumidos) | docs/superpowers/specs/2026-07-04-sp4a-puntuaciones-proyecciones-rankings-design.md · docs/superpowers/plans/2026-07-04-sp4a-puntuaciones-proyecciones-rankings.md | contracts/http/puntuaciones-api.md · contracts/events/operaciones-sesion-events.md | Implemented — Puntuaciones 65/65 verde (53 unit + 8 integration + 4 contract); Operaciones de Sesión sin tocar, regresión de seguridad 434/434 verde (357 unit + 29 integration + 48 contract). Round-trip real de RabbitMQ (`RabbitMqProyeccionRoundTripTests`, opt-in vía `RABBITMQ_TEST_HOST=localhost` con broker de `docker compose`) = **PASS (1/1)** en el cierre del slice (condición I-1 del review final whole-branch cumplida); suite tras la ola de fixes: 66/66 (54 unit + 8 integration + 4 contract). Review final whole-branch (opus) = **READY TO INTEGRATE** (0C/1I-verificación-cumplida/5m; fixes pre-merge aplicados en `ff8a1bd`; deuda anotada en `services/puntuaciones/service-context.md`). **Diferido:** consolidado+team-performance→SP-4b, SignalR ranking→SP-4c, auditoría/historial→SP-4d, publisher propio de Puntuaciones y outbox→ADR-0012, cableado clientes→SP-5. |
| Puntuaciones — ranking consolidado + rendimiento de equipo (SP-4b) | Sobre las proyecciones SP-4a, dos queries on-read: `GET /puntuaciones/partidas/{partidaId}/ranking-consolidado` (solo partida `Terminada`; 409 en otro estado, 404 desconocida) ordena por juegos ganados DESC → puntos totales DESC → tiempo total ASC (ganador por juego = más puntos, desempate menor tiempo, empate exacto → juego sin victoria; empate triple comparte posición 1,2,2,4) y `GET /puntuaciones/equipos/{equipoId}/rendimiento` (partidas Equipo terminadas con ≥1 marcador del equipo, posición + `gano`, `fechaFin DESC`, lista vacía si nada). Un solo `CalculadorRankingConsolidado` reusado por ambas (RF-44 sin duplicar cálculo), tolerante a scoring tardío y juegos no proyectados. Participación = tener marcador (competidores con 0 puntos no aparecen — sin evento de inscripción en el broker). Deuda SP-4a saldada: `xmin` token de concurrencia en `marcadores` (`Property<uint>("xmin").IsRowVersion()`) + reintento único del worker ante `DbUpdateException` (clase base — cubre conflicto xmin en UPDATE y carrera de INSERT por clave única). Sin eventos nuevos ni cambios de esquema (`xmin` es columna de sistema de PostgreSQL preexistente; la migración es no-op de solo snapshot). | Puntuaciones | — | docs/superpowers/specs/2026-07-05-sp4b-consolidado-rendimiento-equipo-design.md · docs/superpowers/plans/2026-07-05-sp4b-consolidado-rendimiento-equipo.md | contracts/http/puntuaciones-api.md | Implemented — 96/96 verde (74 unit + 15 integration + 7 contract). Round-trip real de RabbitMQ no re-corrido en este cierre (Docker Desktop no disponible en el entorno; opcional per plan, ya verificado PASS en el cierre de SP-4a). **Fuente:** RF-44, RF-45, RB-40/RB-41, HU-49, HU-50. **Diferido:** SignalR ranking (incl. `RankingConsolidadoCalculado`)→SP-4c, auditoría/historial→SP-4d, publisher propio y outbox→ADR-0012, cableado clientes→SP-5. |
| Puntuaciones — SignalR de ranking en vivo (SP-4c) | Hub `RankingHub` (`puntuaciones/hubs/ranking`, `[Authorize]`) con `SuscribirAPartida`/`DesuscribirDePartida` (grupo `puntuaciones-partida-{partidaId}`; `HubException("Partida no proyectada.")` si la partida no existe en las proyecciones); difusión best-effort (ADR-0012) de `RankingTriviaActualizado`/`RankingBDTActualizado` (tras proyectar `PuntajeTriviaIncrementado`/`EtapaBDTGanada`) y `RankingConsolidadoCalculado` (tras proyectar `PartidaFinalizada`), orquestada por el worker (`ProyeccionPipeline` → `RankingBroadcastDispatcher` → `IRankingRealtimePublisher`) solo después de una proyección exitosa, a lo sumo un push por mensaje (intento fallido no difunde; el reintento sí lo hace tras tener éxito). Hardening: `[Authorize]` a nivel de clase en `RankingsController`/`EquiposController` y en el hub; `/health` anónimo; JWT del hub vía query string `access_token` solo para rutas `/puntuaciones/hubs/ranking`. | Puntuaciones | — | docs/superpowers/specs/2026-07-06-sp4c-signalr-ranking-vivo-design.md · docs/superpowers/plans/2026-07-06-sp4c-signalr-ranking-vivo.md | contracts/http/puntuaciones-api.md · contracts/events/operaciones-sesion-events.md | Implemented — 119/119 verde (88 unit + 18 integration + 13 contract). Round-trip real de RabbitMQ no re-corrido en este cierre (Docker no disponible; PASS previo en cierre SP-4a). **Fuente:** RF-13, RF-22, RF-37, RF-38, RNF-03, RNF-17, RNF-21, HU-26, HU-42. **Diferido:** auditoría/historial→SP-4d, publisher propio y outbox→ADR-0012, cableado clientes→SP-5. |
| Puntuaciones — historial/auditoría de partida + historial de partidas jugadas (SP-4d) | Segunda cola dedicada `puntuaciones.operaciones-sesion.historial` (durable, binding catch-all `operaciones-sesion.#`, `HistorialEventsConsumer` con el mismo esqueleto que el consumidor de proyecciones SP-4a, `HistorialEventMapper` traduce los **17** tipos del contrato al comando genérico `ProyectarEventoHistorialCommand`) proyecta la tabla nueva `eventos_historial` (`EventId` con índice único como dedup propio — sin tocar `eventos_procesados`; `UbicacionActualizada` muestreada al escribir, máx. 1 por participante por minuto; best-effort ack-siempre ADR-0012). Dos lecturas nuevas: `GET /puntuaciones/partidas/{partidaId}/historial` (HU-43, operador: relato cronológico `occurredAt ASC` con `limit`/`offset`/`tipo`, 404 si la partida no está proyectada, autorización **solo `Operador`/`Administrador`** — primer endpoint de Puntuaciones con chequeo de rol propio, 403 a `Participante`) y `GET /puntuaciones/participantes/{participanteId}/historial-partidas` (HU-27/RF-24, cualquier rol autenticado: historial único de partidas `Terminadas` con `puntosTotales`/`posicion`/`gano` del mismo `CalculadorRankingConsolidado` de SP-4b — RF-44 sin duplicar cálculo —, participación = marcador propio en Individual o membresía resuelta del historial con ≥1 marcador del equipo, orden `fechaFin DESC`). Deudas SP-4a saldadas: índice en `ProcesadoAt` + `PurgaEventosProcesadosService` (retención configurable, default 30 días, nunca purga `eventos_historial`); `ArgumentException`→400 ahora con `LogWarning` (mensaje + path) en el middleware. **Limitaciones documentadas:** invitaciones de equipo ausentes del historial (Identity no publica al broker); integrante de equipo que jamás autoró una acción de juego no ve la partida en su historial; partidas `Canceladas` excluidas del historial de partidas jugadas (RB-30). Con esto la serie SP-4 queda completa. | Puntuaciones | — | docs/superpowers/specs/2026-07-06-sp4d-historial-auditoria-design.md · docs/superpowers/plans/2026-07-06-sp4d-historial-auditoria.md | contracts/http/puntuaciones-api.md · contracts/events/operaciones-sesion-events.md | Implemented — 187/187 verde (139 unit + 28 integration + 20 contract). **Fuente:** RF-12, RF-24, RF-35, RF-37, RB-15, RB-30, RB-31, HU-27, HU-43. **Diferido:** unit tests de las ramas warn+ack del worker de la era SP-4a (deuda anotada, no bloqueante); cableado de clientes web/móvil→SP-5. |
| Autorización — enforcement coarse-role en gateway + permisos funcionales composite en servicios (SP-5a) | Cierra la deuda SP-3a §12 (coarse-role del gateway + permisos funcionales `GestionarPartidas`/`ParticiparEnPartidas` diferidos) y la nota forward-looking de SP-3g (Identity bajo su propio prefijo). ADR-0013: los permisos funcionales (`GestionarPartidas`, `GestionarEquipos`, `ParticiparEnPartidas`) son realm roles técnicos composite de Keycloak sobre los 3 roles base (Operador→GestionarPartidas; Participante→GestionarEquipos+ParticiparEnPartidas; Administrador→ninguno, per BR-R03), expandidos automáticamente por Keycloak en `realm_access.roles` — cero mappers custom; verificado por `scripts/check-realm-composites.py`. Gateway: matriz de 6 rutas por prefijo (`/identity/users`→`Administrador` Order 1; `/identity/teams`→`Participante` Order 1; `/identity/{resto}`→Default Order 2; `/partidas`→`OperadorOAdministrador` nueva policy; `/operaciones-sesion`→Default; `/puntuaciones`→Default, política fina diferida post-SP-4), reenvío puro sin transforms. Operaciones de Sesión: gana normalizador `KeycloakRoleClaims` (antes `RoleClaimType "roles"` sin poblar) + policies `GestionarPartidas` (7 endpoints) / `ParticiparEnPartidas` (10) / autenticado (4 GET) / anónimo (health). Partidas: JWT Keycloak wireado por primera vez (antes cero auth) + policy `GestionarPartidas` en las 3 mutaciones (`POST /partidas`, `POST .../juegos/trivia`, `POST .../juegos/bdt`); `GET /partidas/{id}` autenticado sin permiso — preserva la llamada interna de Operaciones reenviando el bearer del participante (SP-3a §12). Identity: re-homing `api/identity/users`→`identity/users` (policy `AdminOnly`) y `api/teams`→`identity/teams` (swap `ParticipantOnly`→policy `GestionarEquipos`, BR-R02 literal: el permiso autoriza, no el rol). Clientes: mobile (`teams`/`invitations`) y frontend web (`identityApi.ts`) actualizados al nuevo prefijo `identity/`; base URL directa se mantiene en ambos (base URL única del gateway es slice posterior, D6) | Gateway · Identity · Partidas · Operaciones de Sesión | — (Puntuaciones intocado, diferido post-SP-4; gobernanza dinámica de permisos-por-rol→SP-5b; UI web del panel→SP-5c) | docs/superpowers/specs/2026-07-03-sp5a-autorizacion-enforcement-design.md (988a366 + enmienda a53d69e) · docs/superpowers/plans/2026-07-03-sp5a-autorizacion-enforcement.md (69ec98d) | contracts/http/identity-api.md · contracts/http/partidas-config.md · contracts/http/operaciones-sesion-api.md · contracts/http/gateway-api.md · gateway/gateway-context.md · docs/05-decisions/ADR-0013-permisos-funcionales-composites-keycloak.md | Implemented — suite verde: Gateway (14 integration) · Operaciones de Sesión (357 unit + 29 integration + 70 contract) · Partidas (94 unit + 5 integration + 13 contract) · Identity (144 unit + 37 integration + 30 contract) · mobile (94 tests + typecheck) · frontend (53 tests). **Commits:** e174f4c (T1 realm composites) · b4d10de (T2 matriz coarse-role gateway) · 79c3a9a (T3 normalizador + policies Operaciones) · 75dde83 (T4 matriz de permisos por endpoint Operaciones) · f951cbb (T5 JWT + `GestionarPartidas` en Partidas) · a186834 (T6 re-homing Identity + `GestionarEquipos`) · d501346 (T7 clientes mobile/web re-homed) · este commit (T8 ADR-0013 + contratos con autorización + gateway-context + traceability). **Diferido:** gobernanza dinámica permisos-por-rol y cambio de rol de usuario (Identity DB + Keycloak Admin API)→SP-5b, UI web del panel de gobernanza→SP-5c, política fina del prefijo `/puntuaciones` + token WS del hub de rankings→post-SP-4, base URL única del gateway en clientes→slice posterior (D6). |
| Gobernanza backend — permisos por rol + cambio de rol + broker RabbitMQ Identity (SP-5b) | Panel de gobernanza backend (AdminOnly): `GET /identity/governance/roles` expone la matriz rol→permisos con `privilegiosGobernanza` informativo; `PUT /identity/governance/roles/{rol}/permisos` reemplaza el set de permisos funcionales de un rol por diff Keycloak-first (composites add/remove antes de persistir en `permisos_rol`, 502 si Keycloak falla, PUT re-ejecutable e idempotente); `PATCH /identity/users/{userId}/role` cambia el rol de un usuario (Keycloak realm-role swap antes de persistir) — target `Administrador` inmutable (409, BR-R04), 409 si el usuario tiene equipo activo, no-op 200 en mismo-rol, promoción a Administrador permitida e irreversible. Backbone RabbitMQ de Identity: `IIdentityEventsPublisher` unificado (antes solo eventos de equipos) gana `PublishRolUsuarioModificadoAsync`/`PublishPermisosRolActualizadosAsync`; exchange `umbral.identity` topic durable, 6 routing keys explícitas (`IdentityEventRouting`), envelope camelCase `{eventId, eventType, version, occurredAt, payload}`, best-effort post-save (ADR-0012). Dominio: tabla `permisos_rol` con seed BR-R03 (Administrador→sin permisos + privilegios de gobernanza; Operador→GestionarPartidas; Participante→GestionarEquipos+ParticiparEnPartidas) y `Usuario.CambiarRol` con guard de admin inmutable. Gateway: ruta `identity-governance` (`/identity/governance/{**catch-all}`, Order 1, policy `Administrador`) | Identity · Gateway | — (Puntuaciones consumirá los eventos de gobernanza en SP-4; UI web del panel → SP-5c) | docs/superpowers/specs/2026-07-04-sp5b-gobernanza-backend-design.md (5128bf0) · docs/superpowers/plans/2026-07-04-sp5b-gobernanza-backend.md (a55bcb2) | contracts/events/identity-events.md · contracts/http/identity-api.md · contracts/http/gateway-api.md · gateway/gateway-context.md | Implemented — suite verde: Identity (167 unit + 41 integration + 41 contract) · Gateway (17 integration). **Commits:** 57e767f (G1 backbone RabbitMQ Identity + publisher unificado) · 68f50e4 (G2 dominio `permisos_rol` con seed BR-R03 + `Usuario.CambiarRol` guard admin) · 2587c31 (G3 adapter Keycloak gana composites add/remove + cambio de realm role) · e215275 (G4 panel de permisos — GET matriz + PUT set completo con diff Keycloak-first) · e456a31 (G5 cambio de rol de usuario — nunca-admin, 409 equipo activo, Keycloak-first) · 7ee69b5 (G6 sub-ruta `identity-governance` con política `Administrador` en gateway) · este commit (G7 contrato de eventos con §Transport + governance API + GUIA + traceability). **Diferido:** UI web del panel de gobernanza → SP-5c; `UsuarioCreado`/`CredencialTemporalEmitida` + email vía broker (hoy SMTP directo) → slice de audit/notificaciones futuro; consumidor de eventos de gobernanza en Puntuaciones → SP-4; reconciliación automática DB↔Keycloak al arranque → no implementada (el PUT re-ejecutable por diff basta, documentado en 5.2); drift Keycloak↔DB tras un 502 parcial del PUT → riesgo aceptado, reparable re-ejecutando el mismo PUT; `ChangeUserRealmRoleAsync` no atómico (remove+assign en 2 llamadas) → ventana mínima admin-driven, reparable con reintento del PATCH. |
| Gobernanza UI web — panel de permisos por rol + cambio de rol desde gestión de usuarios (SP-5c) | Superficie web (solo `Administrador`) para consumir el backend de gobernanza de SP-5b, sin cambios de contrato/HU. Página **Gobernanza** (`/identidad/gobernanza`): matriz rol→permisos (`GestionarPartidas` / `GestionarEquipos` / `ParticiparEnPartidas`) leída de `GET identity/governance/roles`, guardado por card enviando el set completo marcado a `PUT identity/governance/roles/{rol}/permisos` (el diff es server-side), `privilegiosGobernanza` mostrado como informativo/no-editable. **Cambio de rol** desde Gestión de usuarios: modal en `UserManagementPage` que llama `PATCH identity/users/{userId}/role`, con confirmación reforzada (segundo click "Entiendo, promover") cuando el destino es `Administrador`, fila de `Administrador` sin acción (BR-R04, target admin inmutable) y manejo de 409 (usuario con equipo activo). Rediseño visual + IA únicamente: sin `label`/`id`/`data-testid`/rol usados por tests. | Frontend (web) | Identity · Gateway (consumidos vía cliente `identityApi.ts`, sin cambios de contrato ni de ruta) | docs/superpowers/specs/2026-07-04-sp5c-gobernanza-ui-web-design.md (1be7dec) · docs/superpowers/plans/2026-07-04-sp5c-gobernanza-ui-web.md (366e000) | contracts/http/identity-api.md · contracts/http/gateway-api.md · docs/02-project-context/design/frontend-redesign-plan.md · docs/02-project-context/design/design-system.md | Implemented — suite frontend verde: **67 tests / 11 archivos** · `tsc --noEmit` limpio · `vite build` OK. **Commits:** eb507c8 (H1 API client de gobernanza — roles, permisos por rol y cambio de rol) · b8fe27c (H2 página Gobernanza — matriz de permisos por rol con guardado por card) · 0195ea0 (H3 cambio de rol desde gestión de usuarios — modal con confirmación reforzada para promover a admin) · este commit (H4 docs — traceability + estado del redesign plan). **Diferido:** ninguno funcional nuevo (slice cierra la deuda "UI web del panel de gobernanza → SP-5c" dejada por SP-5a/SP-5b); pase visual en navegador con datos reales queda pendiente igual que el resto de Fase 1 (`frontend-redesign-plan.md`). |
| Fundación gateway + identidad/equipos a través del gateway (Bloque 2a) | RNF-21 (parcial: identity/equipos vía gateway). Gateway YARP configurado con CORS en el borde (`AllowedOrigins`, `AllowedMethods`, `AllowedHeaders`) + Identity CORS retirado. Web carga `VITE_GATEWAY_BASE_URL` para consumir identity/equipos vía :5080 (no directo a :5000); Mobile igual con `EXPO_PUBLIC_GATEWAY_BASE_URL`. E2E: preflight OPTIONS a `/identity/users` → 204 (CORS); POST con rol → 403/ok según permiso; todo vía :5080 ✓ | Gateway · Identity · Frontend · Mobile | — | docs/superpowers/specs/2026-07-07-bloque2a-fundacion-gateway-design.md | contracts/http/gateway-api.md · contracts/http/identity-api.md | Implemented — suite verde: Gateway (20 integration) · Identity (249 unit + 42 integration + 68 contract) · Frontend (67 tests) · Mobile (94 tests). **Fuente:** RNF-21 (parcial: identity/equipos). **Commits:** d2bf5e7 (CORS gateway + CORS off Identity, VITE_GATEWAY_BASE_URL) · 48cc636 (EXPO_PUBLIC_GATEWAY_BASE_URL, E2E preflight vía :5080 PASS). **Diferido:** permisos funcionales en gateway + Identity DB (`GestionarEquipos`/`ParticiparEnPartidas`) → SP-5a; base URL única del gateway en clientes → D6 futuro. |
| Creación/configuración de partidas multi-juego en web (Bloque 2b) | HU-45 (crear partida multi-juego secuencial) + HU-13 (juego Trivia con preguntas) + HU-28 (juego BDT con etapas) — lado configuración/web. UI wizard 3 pasos (datos partida → preguntas Trivia → etapas BDT), validadores encadenados, draft y envío con reintento, lista y detalle de partidas creadas. | Partidas · Frontend (web) | Gateway | docs/superpowers/specs/2026-07-07-bloque2b-web-config-partidas-design.md | contracts/http/partidas-config.md · contracts/http/gateway-api.md | Implemented — suite frontend verde: **96 tests** (tras retiro de 20 tests de páginas viejas Trivia/BDT); E2E de contrato vía gateway :5080 con token operador PASS (POST /partidas + POST .../juegos/trivia + POST .../juegos/bdt → GET /partidas/{id} + GET /partidas, 409 en orden duplicado, 403 de participante). **Commits:** 944f79d (C1 partidasApi cliente vía gateway) · 23cc1e2 (C2 draft partida con validadores) · 73d0b20 + e5b33e4 (C3 envío encadenado con reintento de restantes + fix mensaje de red) · 56759ee (C4 wizard 3 pasos: datos + Trivia + BDT) · f01ba4a (C5 lista y detalle) · 6d05350 + 1afa31b (C6 retiro config vieja trivia/bdt + fix empty-state). **Diferido:** cableado clientes web/móvil de endpoints nuevos de Partidas (endpoints server ya vía SP-2)→ D7. |
| Consola de sesión del operador en web: publicación, lobby, inicio (Bloque 2c-1) | HU-14 (publicar partida → lobby) + HU-15/16 (inicio manual/automático) + HU-18 (visibilidad del estado de la sesión) — lado operador/web. Ruta nueva `/partidas/:id/sesion`: publicar desde el detalle (409 ya-publicada navega igual), panel de lobby (inscritos con refetch 5s + botón, equipos en modalidad Equipo), inicio manual con guard anti doble-click / countdown automático según `modoInicioPartida`, `estado=Cancelada` del inicio (mínimos no alcanzados) tratado como resultado válido, shell post-inicio con juego actual resaltado; tiempo real vía SignalR (`useSesionHub`: PartidaEnLobby/PartidaIniciada/PartidaCancelada/JuegoActivado/PartidaFinalizada, resuscripción en reconnect). | Operaciones de Sesión · Frontend (web) | Gateway · Partidas (config handoff) | docs/superpowers/specs/2026-07-08-bloque2c1-web-sesion-operador-design.md | contracts/http/operaciones-sesion-api.md (§Realtime/SignalR) · contracts/http/gateway-api.md | Implemented — suite frontend verde: **116 tests** (19 files); `tsc -b` limpio (gate real; `tsc --noEmit` pelado es no-op en este repo). E2E vivo vía gateway :5080 con token operador PKCE PASS: POST publicacion → 201+LobbyDto (409 sin juegos, 404 lobby sin publicar observados) · GET lobby/estado 200 shapes exactos · POST inicio sin mínimos → **200 estado=Cancelada** · GET estado → Cancelada · participante POST publicacion → 403. Smoke SignalR PASS: WS por el gateway (`/operaciones-sesion/hubs/sesion`), SuscribirAPartida + POST inicio → push `PartidaCancelada {motivo:MinimosNoAlcanzados}` recibido en la conexión. **Commits:** c4e9166 (T1 operacionesApi) · 7277c76 (T2 @microsoft/signalr + sesionHub) · 35421d1 + 1776fa8 (T3 useSesionHub + fix catch en stop) · 9ce4ac4 + 3ea8ba3 (T4 SesionOperadorPage + fix typecheck real) · 5c149b6 (T5 publicar+ruta+nav). **Diferido:** runtime per-juego (pregunta/etapa/avance) → 2c-2/2c-3; pistas + mapa geoloc → 2c-3; consolidado + retiro páginas legacy trivia/bdt → 2c-4; test del interval 5s con fake timers y rama Equipo del lobby → review final/futuro. |
| Runtime Trivia del operador en la consola de sesión (Bloque 2c-2) | HU-18 (visibilidad de la sesión) + HU-20/21 (sincronización/avance de preguntas Trivia + ranking del juego) — lado operador/web. En `/partidas/:id/sesion`, cuando el juego actual es Trivia+Activo se monta `TriviaRuntimePanel`: pregunta activa (texto + opciones con la **correcta marcada** cruzando config por `opcionId`), countdown a `fechaLimiteUtc`, botón "Cerrar y avanzar" (`avanzarPregunta`), estado "sin pregunta activa" (409) con botón "Finalizar juego" (`finalizarJuegoActual` → `terminada`/`onJuegoAvanzado`), y **ranking del juego en vivo** por `GET /puntuaciones/.../ranking` refetcheado en las señales del hub de sesión (`PreguntaActivada`/`PreguntaCerrada`) — sin segundo hub SignalR. Paga arrastres de 2c-1: **F1** seq-guard en `cargar()` (+ bump en pushes terminales para invalidar cargas en vuelo) y **F5** pills de juego por `estado` real. Juego BDT actual → placeholder de 2c-3. | Operaciones de Sesión · Puntuaciones · Frontend (web) | Gateway · Partidas (config handoff) | docs/superpowers/specs/2026-07-08-bloque2c2-web-runtime-trivia-operador-design.md | contracts/http/operaciones-sesion-api.md · contracts/http/puntuaciones-api.md · contracts/http/gateway-api.md | Implemented — suite frontend verde: **133 tests** (21 files); `tsc -b` limpio. E2E vivo vía gateway :5080 (tokens operador+participante PKCE, stack partidas+operaciones+**puntuaciones**+rabbitmq+gateway) PASS: crear+2 preguntas → publicar 201 → inscribir participante 201 → iniciar 200 Iniciada → `GET /pregunta-actual` 200 → participante responde correcto → `esCorrecta:true,cerroPregunta:true,puntaje:10` → `GET /puntuaciones/.../ranking` 200 con `puntos:10` (proyección RabbitMQ inmediata) → pregunta 2 se autoactiva al cerrar la 1 → `POST /pregunta-actual/avance` `sinMasPreguntas:true` → `GET /pregunta-actual` **409** → `POST /juego-actual/finalizacion` `terminada:true` → participante `POST /pregunta-actual/avance` **403**. **Cruce opcionId config↔runtime verificado idéntico en vivo** (fallback por texto = red de seguridad). Smoke SignalR PASS: operador suscrito recibe `PreguntaActivada {fechaLimiteUtc}` + `PreguntaCerrada` vía gateway. **Commits:** 6518614 (T1 operacionesApi runtime) · 6b42149 (T2 puntuacionesApi) · 51f2ff7 (T3 useSesionHub +2 eventos) · 2bcc19b (T4 TriviaRuntimePanel) · ff08290 + 64da651 (T5 integración página + fix invalidación de cargas en vuelo). **Diferido:** hub ranking SignalR SP-4c (GET por señal alcanza para Trivia); runtime BDT + pistas + geoloc → 2c-3; consolidado + retiro legacy → 2c-4; resolución de nombres de competidor (contrato sin nombres → GUID corto). |
| Runtime BDT del operador en la consola de sesión (Bloque 2c-3) | HU-18 (visibilidad de la sesión) + HU-22/23 (avance de etapas + entrega de pistas) + HU-24 (mapa de geolocalización) — lado operador/web. En `/partidas/:id/sesion`, cuando el juego actual es BDT+Activo se montan 3 paneles: `BdtRuntimePanel` (etapa activa con `areaBusqueda` + countdown a `fechaLimiteUtc`, "Cerrar y avanzar" `avanzarEtapa`, "sin etapa activa" 409 → "Finalizar juego", ranking del juego = Σ Puntaje de etapas ganadas por GET disparado en señales del hub), `PistasPanel` (selector de roster `GET /lobby` → `enviarPista` a participante/equipo según modalidad), `GeoMapPanel` (react-leaflet + `CircleMarker` por participante, alimentado por `UbicacionActualizada` que la página acumula en un `Map`). El operador **no** valida QR (participant-safe: `EtapaActualDto` sin `codigoQREsperado`). Reusa F1 seq-guard / F5 pills. | Operaciones de Sesión · Puntuaciones · Frontend (web) | Gateway · Partidas (config handoff) | docs/superpowers/specs/2026-07-08-bloque2c3-web-runtime-bdt-operador-design.md | contracts/http/operaciones-sesion-api.md (§Realtime/SignalR) · contracts/http/puntuaciones-api.md · contracts/http/gateway-api.md | Implemented — suite frontend verde: **150 tests** (24 files); `tsc -b` limpio. Deps nuevas: `leaflet@1.9` + `react-leaflet@4.2.1` (v4 pin — v5 exige React 19). E2E vivo vía gateway :5080 (tokens operador+participante PKCE, stack partidas+operaciones+puntuaciones+rabbitmq+gateway) PASS: crear juego BDT (2 etapas, `areaBusqueda` juego + `codigoQREsperado`/`puntaje` por etapa) → publicar 201 → inscribir participante 201 → iniciar 200 → `GET /etapa-actual` 200 **sin `codigoQREsperado`** → `POST /pistas` a participante 200 (`PistaEnviadaResponse`) → `POST /etapa-actual/avance` `etapaActivadaOrden:2` → avance 2 `sinMasEtapas:true` → `GET /etapa-actual` **409** → `POST /juego-actual/finalizacion` `terminada:true` → participante `POST /etapa-actual/avance` **403** + `POST /pistas` **403**. Smoke SignalR PASS: operador suscrito recibe `EtapaActivada {fechaLimiteUtc}` + `EtapaCerrada`, y **`UbicacionActualizada {latitud:10.5, longitud:-66.9}`** tras `EnviarUbicacion` del participante (grupo `operador:partida:{id}` end-to-end). **Commits:** 5ccb726 (T1 operacionesApi etapa/avance/pistas) · 5453f9d (T2 useSesionHub +4 eventos) · 5871b23 (T3 leaflet + GeoMapPanel) · d622fd2 (T4 BdtRuntimePanel) · 237640f (T5 PistasPanel) · 7dbd692 (T6 integración página). **Diferido:** consolidado + retiro páginas legacy `trivia/operar`+`bdt/partidas` → 2c-4; hub ranking SignalR SP-4c; resolución de nombres (GUID corto); click-en-marcador para pista (descartado). |
| Ranking consolidado en la consola + retiro de páginas legacy (Bloque 2c-4 — **cierra el Bloque 2c**) | RF-45 (ranking consolidado al finalizar) lado operador/web + housekeeping. La vista `terminada` de `/partidas/:id/sesion` monta `ConsolidadoPanel`: `GET /puntuaciones/partidas/{id}/ranking-consolidado` con **reintento corto** (3 intentos, 1.5s, solo ante 404/409 — lag de proyección tras `PartidaFinalizada`), tabla posición · competidor (GUID corto) · juegos ganados · puntos totales · tiempo total, `entradas:[]` → "Sin resultados", persistente → "Consolidado no disponible aún" + Reintentar. **F-2**: `runtimeShared.tsx` extrae `Countdown`/`RankingView`/`formatTiempo` (antes triplicados en TriviaRuntimePanel + BdtRuntimePanel + página; testids intactos vía props). **Retiro legacy**: eliminadas `features/trivia/TriviaOperationsPage` + `features/bdt/PublishedBdtGamesPage` + `api/triviaApi` + `api/bdtApi` (+tests), rutas `trivia/operar`+`bdt/partidas` y áreas de nav — toda la operación vive en la consola de sesión (−1528 líneas). | Puntuaciones · Frontend (web) | Gateway | docs/superpowers/specs/2026-07-08-bloque2c4-web-consolidado-retiro-legacy-design.md | contracts/http/puntuaciones-api.md (§ranking-consolidado) · contracts/http/gateway-api.md | Implemented — suite frontend verde: **138 tests** (22 files; 157 pre-retiro − 19 de páginas legacy); `tsc -b` limpio; `npm run build` PASS (leaflet bundlea CSS+JS — **F-1** lado build). E2E vivo vía gateway :5080 (tokens operador+participante PKCE, stack completo) PASS: consolidado **409** antes de Terminada → flujo completo (crear/publicar/inscribir/iniciar/responder correcto `puntaje:10`/finalizar `Terminada`) → `GET /ranking-consolidado` **200** con `entradas: [{posicion:1, juegosGanados:1, puntosTotales:10, tiempoTotalMs}]` — shape exacto al contrato. **Commits:** dc3adb9 (T1 puntuacionesApi getRankingConsolidado) · 11392d9 (T2 runtimeShared F-2) · b3793c4 (T3 ConsolidadoPanel + vista terminada) · 739036a (T4 retiro legacy). **Diferido:** hub ranking SignalR SP-4c; resolución de nombres (GUID corto); smoke visual leaflet en navegador (opcional, riesgo bajo). **BLOQUE 2C COMPLETO** (2c-1 consola/lobby/inicio · 2c-2 runtime Trivia · 2c-3 runtime BDT/pistas/geoloc · 2c-4 consolidado/retiro legacy). |
| CI + cobertura backend + compose completo (Bloque 6) | RNF-11: pipeline GitHub Actions (`.github/workflows/ci.yml`) — 8 jobs paralelos (5 soluciones .NET en matrix con `--collect:"XPlat Code Coverage"` + frontend `npm test`+`build` + mobile `npm test`+`typecheck` + agregador `cobertura` con reportgenerator → tabla por servicio + total en el step summary, report-only sin gate) disparado por push a develop/master y todo pull request. RNF-09: primera medición de cobertura del proyecto — línea base 2026-07-07: identity 70.7% · puntuaciones 52.3% · partidas 50.4% · operaciones-sesion 38.1% · gateway 25.2% · **total backend 48.1%** (5979/12407 líneas, cifra cruda sin exclusiones — Migrations/Program.cs inflan el denominador). RNF-10: compose completo — Dockerfile de identity (faltante), init SQL de las 4 bases (`infra/postgres-init/`), env Keycloak/RabbitMQ/URLs inter-servicio y puertos 5010/5020/5030 en las entradas existentes, healthchecks postgres/rabbitmq + `depends_on: service_healthy` (arranque DB-fresca sin carrera), migraciones EF al arranque gated por `EF_MIGRATE_ON_STARTUP` (default off; solo compose; identity SIN hook — su bootstrap `EnsureCreated` preexistente cubre DB fresca y `Migrate()` la corto-circuitaba, Critical del review T4 corregido) | — (transversal: infra + los 4 servicios + gateway + clientes en CI) | — | docs/superpowers/specs/2026-07-06-bloque6-ci-cobertura-compose-design.md · docs/superpowers/plans/2026-07-06-bloque6-ci-cobertura-compose.md | — (sin cambios de contratos HTTP/eventos) | Implemented — corrida real en GitHub Actions (run 28861490571, PR draft a develop): **8/8 jobs SUCCESS**; bring-up local volumen-fresco: 8/8 contenedores Up, realm 200, /health 200, 12 migraciones aplicadas, 0 fatales. **Commits:** fa45c60 (T1 ci.yml) · 4fa3e77 (T2 Dockerfile identity) · 3ea79cb + c381786 (T3 compose+init, fix clave inerte) · 39a4495 + d1c8fc5 (T4 hook migraciones, fix identity sin hook) · 8adc02e (T5 healthchecks) · db86731 (T5 GUIA) · este commit (T6 traceability + línea base). **Diferido:** gate duro de cobertura (decidir con línea base a la vista), exclusiones de cobertura (Migrations/), build de imágenes en CI, retiro de entradas legacy del compose → Bloque 3. |
| Panel mobile de partidas + participación completa + SignalR (Bloque 2d) | HU-19 (unirse a partida) + HU-25/26 (convocatorias de equipo) + panel único "Partidas" con filtro de modalidad — lado participante/mobile, previo al gameplay (2e). **Backend:** endpoint nuevo `GET /operaciones-sesion/partidas-publicadas` (Operaciones de Sesión) — listado participant-safe de sesiones en `Lobby` (`PartidaPublicadaDto`: nombre/modalidad/modo inicio/cupos/inscritosActivos; sin juegos, preguntas ni QR; autenticado sin policy como las lecturas compartidas). **Mobile** (feature nuevo `features/partidas/`): 4 api modules result-object (publicadas/inscripciones+equipo/convocatorias/mi-sesión) + `sesionHub` (`@microsoft/signalr@^8`, mismo hub vía gateway) + `PartidasPanelScreen` (filtro Todas/Individual/Equipo, pull-refresh, banner mi-sesión) + `PartidaLobbyScreen` (inscribirse/cancelar Individual, preinscribir/cancelar Equipo-líder, push `PartidaEnLobby`→refetch / `PartidaIniciada` / `PartidaCancelada`) + `ConvocatoriasScreen` (inbox aceptar/rechazar) + Home renovado. **Retiro legacy mobile:** `features/trivia`+`features/bdt`+`api/triviaApi`+10 tests+rutas+vars env (−4417 líneas). | Operaciones de Sesión · Frontend (mobile) | Gateway · Identity (equipos) · Partidas (config) | docs/superpowers/specs/2026-07-09-bloque2d-mobile-panel-participacion-design.md | contracts/http/operaciones-sesion-api.md (§partidas-publicadas, participación) · contracts/http/gateway-api.md | Implemented — backend Operaciones **360 unit (+3) / 29 int / 70 contract**; mobile **61 tests** (111 pre-retiro − 50 legacy) + `tsc --noEmit` limpio. E2E vivo vía gateway :5080 (tokens PKCE op+participante) PASS: `GET /partidas-publicadas` muestra publicada con shape exacto camelCase, **oculta sin-publicar y desaparece al iniciar** → Individual: inscribir 201 → `mi-sesion` 200 → cancelar 204 → `mi-sesion` 204 → Equipo: crear equipo (`nombreEquipo`) → líder `POST /inscripciones-equipo` 201 (`convocados:1`) → `GET /mis-convocatorias` shape exacto → aceptar 200 `estado:Aceptada`. Smoke SignalR PASS: participante suscrito (signalr de `mobile/node_modules`) recibe `PartidaIniciada` vía gateway. **Commits:** 20efb95 (T1 backend listado) · a094bfc (T2 contrato) · 1ab093b (T3 api modules) · 73d6045 + eb00784 (T4 sesionHub + pin signalr@^8) · 318fffb (T5 PartidasPanel) · d21cd59 (T6 PartidaLobby) · 24be4af (T7 Convocatorias+Home) · 92506ef (T8 retiro legacy). **Diferido:** gameplay mobile (responder/QR/pistas/geoloc) → 2e; `permissions/bdt*` huérfanos (candidatos reuso 2e); RNF-24 refresh 270s → mini-slice; nombre real de partida en banner mi-sesión (payload sin nombre). |
| Gameplay Trivia mobile + estructura live del participante (Bloque 2e-1) | HU-20 (responder Trivia) + estructura live que 2e-2 reusa para BDT — lado participante/mobile, sin cambios backend/contrato. **Mobile** (`features/partidas/`): `gameplayApi.js` (getPreguntaActual con 409→`sin_pregunta` no-fatal, responderPregunta `{opcionId}`, getRankingJuego, getRankingConsolidado) + `partidaLiveFlow.js` (`cargarLive` por mi-sesión → fases sin-participacion/lobby/iniciada con `juegoActivo`+`yaRespondio`) + `liveShared.tsx` (`Countdown`+`RankingTable` pre-extraídos para 2e-2) + `TriviaPlayPanel` (responder UNA vez: resultado propio esCorrecta/puntaje, 409 duplicada→"ya respondida" no-fatal, countdown a fechaActivacion+tiempoLimite, ranking GET-en-señal patrón 2c-2) + `PartidaLiveScreen` (máquina 6 fases: cargando/sin-participación/iniciada[Trivia·BDT placeholder]/finalizada[consolidado + fila propia resaltada por sub del token + Reintentar]/cancelada; hub propio: PreguntaActivada→reset+refetch, PreguntaCerrada→refetch, JuegoActivado→cargarLive, PartidaFinalizada/Cancelada) + ruta `PartidaLive` + navegación lobby→live (`navigation.replace` en PartidaIniciada o lobby ya Iniciada; banner panel rutea por estadoPartida). **Fix minor 2d:** `cargarLobby` gana `esLider` (Equipo → `GET /identity/teams/mine`, `participantes[].esLider` del caller por `sub`; Individual → true sin request; 404/red → false member-safe) — miembro no-líder ve texto informativo en vez del botón de preinscripción. | Operaciones de Sesión · Puntuaciones (lectura) · Frontend (mobile) | Gateway · Identity (teams/mine) | docs/superpowers/specs/2026-07-09-bloque2e1-mobile-gameplay-trivia-design.md · docs/superpowers/plans/2026-07-09-bloque2e1-mobile-gameplay-trivia.md | contracts/http/operaciones-sesion-api.md (§pregunta-actual, respuesta, mi-sesion, SignalR) · contracts/http/puntuaciones-api.md (§ranking, ranking-consolidado) | Implemented — mobile **70 tests** (61+9) + `tsc --noEmit` limpio. E2E vivo vía gateway :5080 (tokens PKCE op+participante, stack completo con puntuaciones) PASS: partida Trivia 2 preguntas → participante `GET /pregunta-actual` 200 **sin campo de correcta** (anti-leak verificado) → respuesta incorrecta 200 `{esCorrecta:false,cerroPregunta:false}` → 2ª respuesta **409 duplicada** → avance → respuesta correcta 200 `{esCorrecta:true,cerroPregunta:true,puntaje:10}` → `GET /ranking` juego **con token participante** 200 `{puntos:10,unidadesGanadas:1}` → finalizar `Terminada` → `GET /ranking-consolidado` participante 200 `{juegosGanados:1,puntosTotales:10}` → higiene mi-sesión 204. Smoke SignalR PASS (cliente signalr@^8 de mobile/node_modules vía gateway): participante recibe `PartidaIniciada`, `JuegoActivado`, `PreguntaActivada {fechaLimiteUtc}` ×2, `PreguntaCerrada` ×2, `PartidaFinalizada`. **Commits:** 4091ef1 (T1 gameplayApi) · cec9165 (T2 partidaLiveFlow) · 161a85c (T3 liveShared+TriviaPlayPanel+PartidaLiveScreen+ruta) · 1668817 (T4 navegación lobby→live+banner) · 7b3922c + 06dd8fa (T5 fix esLider — el fix corrige al shape real `participantes[].esLider` de teams/mine; el review detectó forma legacy `liderUserId`). **Diferido:** BDT (QR/pistas/geoloc) → 2e-2; hub ranking SignalR → SP-4c; RNF-24 → mini-slice; resolución de nombres de competidor (GUIDs). |
| Gameplay BDT mobile — QR + pistas + geolocalización (Bloque 2e-2 — **cierra el Bloque 2e**) | HU gameplay BDT del participante mobile, sin cambios backend/contrato. **Mobile** (`features/partidas/` + `permissions/`): `gameplayApi.js` +2 fns (`getEtapaActual` 409→`sin_etapa` no-fatal; `validarTesoro` `{imagenBase64}` — 200 SIEMPRE ok incluso `Invalido`, reintentos ilimitados por contrato) + `bdtTreasureImagePicker` adaptado (`base64:true`, expone `image.base64`; primeros tests del módulo) + `BdtPlayPanel.tsx` (etapa activa con `areaBusqueda`+countdown, botones Cámara/Galería→picker→validar, gano→success+puntaje / fallo→motivo mapeado sin deshabilitar, pistas de la prop, ranking GET-en-señal) + `PartidaLiveScreen` (handlers `EtapaActivada`→reset+refetch / `EtapaCerrada`/`EtapaGanada`→refetch / `PistaEnviada`→append (JuegoActivado limpia), efecto geoloc: permiso→`watchPositionAsync {timeInterval:2000}`→`hub.invoke("EnviarUbicacion")` best-effort vía `hubRef`, denegado→Notice sin bloquear gameplay, cleanup limpia watcher+aviso). **Minors 2e-1 cerrados:** consolidado muestra columna `juegosGanados` (opcional en `RankingTable`) + reset Trivia limpia error banner. | Operaciones de Sesión · Puntuaciones (lectura) · Frontend (mobile) | Gateway | docs/superpowers/specs/2026-07-10-bloque2e2-mobile-gameplay-bdt-design.md · docs/superpowers/plans/2026-07-10-bloque2e2-mobile-gameplay-bdt.md | contracts/http/operaciones-sesion-api.md (§etapa-actual, tesoro, pistas, SignalR/EnviarUbicacion) · contracts/http/puntuaciones-api.md | Implemented — mobile **74 tests** (70+4) + `tsc --noEmit` limpio. E2E vivo vía gateway :5080 (stack completo, QRs PNG generados con python-qrcode) PASS: partida BDT 2 etapas → `GET /etapa-actual` participante 200 **sin `codigoQREsperado`** (anti-leak) → QR incorrecto 200 `{resultado:"Invalido", gano:false}` ×2 (**sin 409 — reintentos ilimitados verificados**) → QR correcto 200 `{resultado:"Valido", gano:true, cerroEtapa:true, puntaje:50}` → etapa 2 activada → segundo QR `puntaje:30` → ranking juego token participante 200 `{puntos:80, unidadesGanadas:2}` (ranking por puntos, etapas ganadas informativas — regla BDT correcta) → finalizar `Terminada` → consolidado 200 `{juegosGanados:1, puntosTotales:80}` → higiene mi-sesión 204. Smoke SignalR PASS (2 clientes signalr@^8 vía gateway): participante recibe `EtapaActivada {fechaLimiteUtc}`×2, `EtapaGanada`×2, `EtapaCerrada`×2, `PistaEnviada {texto}` (destino correcto), `PartidaFinalizada`; **relay geoloc E2E**: participante `EnviarUbicacion(10.5, -66.9)` → operador recibe `UbicacionActualizada` con esas coordenadas. **Commits:** fd1b4f0 (T1 gameplayApi BDT) · 502e399 (T2 picker base64) · 35771d0 (T3 BdtPlayPanel) · 922ed5a (T4 live screen BDT+geoloc+pistas) · 62f9a2b (T5 minors 2e-1 + limpieza avisoGeo). **Diferido:** `.catch` del IIFE geoloc no gateado por `cancelado` (carrera improbable, pre-existente); hub ranking SignalR → SP-4c; RNF-24 → mini-slice; resolución de nombres (GUIDs). **BLOQUE 2E COMPLETO** (2e-1 Trivia + estructura live · 2e-2 BDT). |
| Rankings en vivo por push SignalR en clientes (Bloque 3a — SP-4c) | Consumo del hub `puntuaciones/hubs/ranking` (backend SP-4, ya operativo) en web y mobile, **aditivo** sobre los GET existentes (fuente recuperable, ADR-0012): el ranking pasa de moverse solo al cerrar pregunta/etapa a actualizarse **intra-pregunta/etapa** (cada `PuntajeTriviaIncrementado`/`EtapaBDTGanada` difunde). **Web:** `api/rankingHub.ts` + `useRankingHub` (espejo de useSesionHub: handlersRef, onreconnected resubscribe, catch silencioso de `HubException "Partida no proyectada."`) montado en `SesionOperadorPage`; props `rankingPush`→`TriviaRuntimePanel`/`BdtRuntimePanel` (aplican si `juegoId` coincide) y `consolidadoPush`→`ConsolidadoPanel`. **Mobile:** `rankingHub.js` (espejo sesionHub) como 2º hub en `PartidaLiveScreen`; prop `rankingPush` a `TriviaPlayPanel`/`BdtPlayPanel` + `RankingConsolidadoCalculado`→consolidado anticipado (GET+Reintentar quedan). Ningún GET retirado. | Puntuaciones (hub existente) · Frontend (web + mobile) | Gateway (passthrough WS) | docs/superpowers/specs/2026-07-10-bloque3a-rankings-push-clientes-design.md · docs/superpowers/plans/2026-07-10-bloque3a-rankings-push-clientes.md | contracts/http/puntuaciones-api.md (§SignalR SP-4c) | Implemented — frontend **159 tests** (+2) + `tsc -b` + build PASS; mobile **75 tests** (+1) + typecheck limpio. Smoke E2E vivo vía gateway :5080 (cliente signalr@^8): conexión WS a `puntuaciones/hubs/ranking` OK; suscripción a partida NO proyectada → `HubException: Partida no proyectada.` capturada sin romper; partida Trivia real: al responder correcto llega `RankingTriviaActualizado {juegoId, entradas:[{puntos:20, unidadesGanadas:1}]}` **antes de avanzar/cerrar** (push intra-pregunta verificado) y al finalizar llega `RankingConsolidadoCalculado {entradas:[{juegosGanados:1, puntosTotales:20}]}`; higiene mi-sesión 204. **Commits:** 814fe57 (T1 web) · 0d4d6b2 (T2 mobile). **Diferido:** race improbable ConsolidadoPanel push-vs-retry (ventana ~4.5s; sugerido receivedPush ref si se toca); sin push de rankings en lobby/detalle (solo consola/live, per spec). |
| UI web de Puntuaciones — historial de partida + rendimiento de equipo (Bloque 2f — **cierra el Bloque 2**) | HU-43 (historial cronológico de una partida para Operador/Administrador) + HU-49/RF-44 (rendimiento histórico de equipo: posición y victoria por partida terminada). `puntuacionesApi.ts` incorpora las dos consultas; `HistorialPartidaPage` ofrece filtro por los 17 tipos canónicos, paginación y enlaces desde detalle/consola; `RendimientoEquipoPage` consulta por GUID y muestra posición/resultado. Rutas web protegidas para Operador y nueva área de navegación `Puntuaciones`; sin cálculo autoritativo en cliente. | Puntuaciones · Frontend (web) | Gateway | docs/superpowers/specs/2026-07-10-bloque2f-web-ui-puntuaciones-design.md · docs/superpowers/plans/2026-07-10-bloque2f-web-ui-puntuaciones.md | contracts/http/puntuaciones-api.md (§historial de partida, §rendimiento de equipo) · contracts/http/gateway-api.md | Implemented — frontend **148/148** verde, `npx tsc -b` limpio y `npm run build` PASS. E2E vivo vía gateway :5080 con JWT Keycloak reales PASS sobre una partida BDT terminada: historial completo `200 total=16`; filtro `tipo=PartidaIniciada` `200 total=1`; `limit=2&offset=0` devuelve 2 entradas; token Participante → **403**; partida inexistente → **404**; equipo aleatorio → `200 {partidas:[]}`. El gate detectó y cerró un defecto preexistente de Puntuaciones: faltaba normalizar `realm_access.roles` hacia `RoleClaimType=roles`; fix TDD `27aeb42`, suite Puntuaciones **188/188**. **Commits UI:** 51ea339 (T1 API) · 50c45f0 (T2 historial) · 14fdcee (T3 rendimiento) · 27aeb42 (prerequisito auth descubierto por E2E). **BLOQUE 2 COMPLETO** (2a gateway · 2b configuración web · 2c operación web · 2d participación mobile · 2e gameplay mobile · 2f consultas Puntuaciones web). |
| Vista web de equipos (Bloque 3b) | Listado de solo lectura de **todos** los equipos (Activo/Desactivado/Eliminado, orden por nombre) para Administrador/Operador con enlace directo al rendimiento (cierra el minor 2f "entrada por equipoId manual"). **Identity:** `GET /identity/teams` nuevo — `IEquipoRepository.GetAllAsync` + `ListarEquiposQuery/Handler` (nombres resueltos joineando por `Usuario.KeycloakId` = sub de Keycloak que guardan los miembros de equipo, **no** por el id local; usuario sin fila local → `""`) + `TeamsAdminController` con policy nueva `OperadorOAdministrador` (fuera de `TeamsController`: su policy de clase `GestionarEquipos` es aditiva). **Gateway:** ruta `identity-teams-listing` (Order 0, path exacto, `Methods:[GET]`, policy `OperadorOAdministrador`); `/mine` y `POST` siguen `Participante`. **Web:** `identityApi.getEquipos` + `EquiposPage` (tabla nombre/badge estado/miembros con líder marcado/enlace `Ver rendimiento`) + área nav "Equipos" ambos roles + ruta `/equipos`; `RendimientoEquipoPage` acepta deep-link `?equipoId=` (prefill + auto-consulta única, GUID inválido → flujo manual). | Identity · Gateway · Frontend (web) | — | docs/superpowers/specs/2026-07-10-bloque3b-vista-web-equipos-design.md · docs/superpowers/plans/2026-07-10-bloque3b-vista-web-equipos.md | contracts/http/identity-api.md | Implemented — Identity **170 unit / 41 integration / 41 contract**; gateway **24** (20+4); frontend **169 tests** + `tsc -b` + build PASS. E2E vivo vía gateway :5080: operador `GET /identity/teams` **200** con miembro `nombre` resuelto en vivo; admin **200**; participante **403**; regresión `GET /identity/teams/mine` **200**. **Commits:** d8709ab (T1 Identity) · b86c1a9 (T2 gateway) · bd1c97e (T3 web) · bff39dc (T4 deep-link) · 1188e21 (fix E2E: join por KeycloakId). **Gotcha:** los usuarios seed del realm no tienen fila en la tabla local `usuarios` — sus nombres salen `""` hasta crearlos por el flujo admin. **Diferido:** guard de unicidad `KeycloakId`; test de integración HTTP de la policy (cubierto vivo por el gate). |
| Refresh de sesión 270s con control de inactividad (Bloque 3c — RNF-24, **cierra el Bloque 3**) | RNF-24: tras autenticarse, el front-end refresca el token de Keycloak cada **270s** (access token 300s, margen 30s) **directo contra Keycloak** (nunca gateway/backend), registrando actividad continua (toques/clicks/scroll/teclado/navegación); inactivo al tick → modal "¿Sigues ahí?" con "Continuar sesión"/"Salir", sin countdown (la sesión SSO idle 30 min decide la expiración real); refresh fallido → logout con aviso. Núcleo puro por cliente (`sessionRefreshCore` TS/JS espejo: tick/marcarActividad/continuar, actividad consumida pre-await, guards modal+reentradas). **Web:** `authProvider.refresh()` (`updateToken(-1)`) + `useSessionRefresh` (listeners pointerdown/keydown/scroll/popstate, `REFRESH_INTERVAL_MS=270_000`) + `SessionExpiryModal` + wiring App (token nuevo → authState inmutable; expiración → flag sessionStorage → aviso en LoginScreen). **Mobile:** `refrescarTokenFlow` puro + `refreshSessionAsync` (SecureStore, rotación de refresh) + scheduler en AuthProvider keyed en existencia de sesión + View wrapper `onStartShouldSetResponderCapture` (toques = actividad sin robar gestos) + modal RN espejo. **Hubs sin churn:** factories web/mobile aceptan `getToken: () => string`; hooks/efectos con tokenRef y token fuera de deps — un refresh NO reconecta conexiones vivas (web useSesionHub/useRankingHub; mobile PartidaLiveScreen×2 y PartidaLobbyScreen). | Frontend (web + mobile) · Keycloak | — (ningún cambio de realm/backend/gateway/contratos) | docs/superpowers/specs/2026-07-10-bloque3c-rnf24-refresh-sesion-design.md · docs/superpowers/plans/2026-07-10-bloque3c-rnf24-refresh-sesion.md | — | Implemented — frontend **178 tests** (28 files, +9) + `tsc -b` + build PASS; mobile **88 tests** (+13) + typecheck limpio. E2E vivo contra Keycloak real: login PKCE participante → access `exp-iat=300s` y refresh 1800s (premisa RNF-24 con defaults del realm, sin override) → `grant_type=refresh_token` directo a :8080 **200** con access nuevo ≠ anterior y refresh rotado → access nuevo válido vía gateway (`/identity/teams/mine` 200) → reuso del refresh viejo: **200** (rotación no estricta, default Keycloak; el cliente siempre usa el último). **Commits:** f41dba4 (T1 core web) · 99b2539 (T2 web refresh+hook+modal+App) · 170697a (T3 hubs web getter) · 1178dfa (T4 mobile flow+core) · ed2888d (T5 scheduler+modal mobile) · 07190ca (T6 hubs mobile getter). **Diferido:** role="dialog" en backdrop vs card (cosmético heredado del brief); refetch de datos por re-render al rotar token (GETs baratos); sin reintentos de refresh con backoff (YAGNI per spec). **BLOQUE 3 COMPLETO** (3a rankings push · 3b vista web equipos · 3c RNF-24). |
| Remediación auditoría de conformidad Bloques 2-3 (R1-R4) | Cierra los 4 hallazgos del informe 2026-07-11 (CONFORME CON RESERVAS 0C/1I/3m). **R1/H1 (Important, D2):** Administrador gana las vistas de operación de partidas en **modo observador** per doctrina "admin views operations read-only" — rutas `partidas`, `partidas/:id` y `partidas/:id/sesion` pasan a `need=["Operador","Administrador"]` (crear sigue Operador), área nav "partidas" para ambos roles con item "Nueva partida" solo Operador (`NavItemDef.roles?` + filtrado de items en `areasForRoles`), y prop `puedeOperar = roles.includes("Operador")` calculada una vez en App que oculta TODA acción mutante: btn-nueva-partida (lista), btn-publicar-operar (detalle), btn-iniciar (consola, countdown automático intacto), avanzar/finalizar en `TriviaRuntimePanel`/`BdtRuntimePanel`, y `PistasPanel` completo; `GeoMapPanel`, lobby, rankings, countdowns y estado siempre visibles. Backend/gateway sin cambios (ya autorizaban). **R2/H2:** fila `identity-teams-listing` en la matriz de `gateway-api.md` (= appsettings real). **R3/H3:** los 4 eventos de ranking en `puntuaciones-events.md` pasan a "Registrado" con cross-ref a `puntuaciones-api.md` §SP-4c (RabbitMQ integration-events siguen SDD-gated). **R4/H4:** `mobile/run-local.sh`/`.ps1` dejan de generar `EXPO_PUBLIC_TRIVIA/BDT_API_BASE_URL` (+ `$BdtPort`/`$TriviaPort` sin uso). | Frontend (web) · Contratos · Scripts mobile | — | docs/superpowers/specs/2026-07-11-remediacion-auditoria-bloques2-3-design.md · docs/superpowers/plans/2026-07-11-remediacion-auditoria-bloques2-3.md · docs/04-sdd/auditorias/2026-07-11-informe-conformidad-bloques2-3.md | contracts/http/gateway-api.md · contracts/events/puntuaciones-events.md | Implemented — frontend **194 tests** (29 files, +12 sobre el baseline 182 de la auditoría) + `tsc -b` + build PASS; mobile **88** + typecheck (smoke, solo scripts). **Commits:** 19dfce0 (T1 rutas+nav) · 7129e35 (T2 lista+detalle) · 566cd50 (T3 consola+paneles) · d965dde (T4 contratos+scripts). Reviews por tarea APPROVED (sonnet, 0C/0I). |
| Retiro físico de servicios legacy (**cierra el Bloque 3 de la auditoría de cobertura**) | Elimina del repo el layout viejo prohibido por CLAUDE.md §"Explicit non-services": `services/trivia-game-service/` y `services/bdt-game-service/` completos (346 archivos trackeados), sus bloques en `infra/docker-compose.yml` (+ comentario de cabecera "in transit"), `TRIVIA_PORT`/`BDT_PORT` de `.env.example`, los pasos de levantamiento legacy de `GUIA-LEVANTAMIENTO.md` (nota actualizada a "retirados" + drop opcional de las DBs residuales `umbral_trivia_game`/`umbral_bdt_game`) y el cliente `bdt-game-service` de los seeds de Keycloak (`umbral-realm.json`, aplica a re-seeds; realm vivo inocuo). Se conservan deliberadamente: pointers de `docs/03-microservices/`, patrones de `scripts/detect-obsolete-doctrine.sh` y docs históricos. Limpieza local sin commit: vars muertas `EXPO_PUBLIC_TEAM/BDT/TRIVIA_API_BASE_URL` fuera de `mobile/.env` y `VITE_TRIVIA/BDT_API_BASE_URL` fuera de `frontend/.env` (gitignored; los run-local ya generan limpios desde la remediación R4). Precondición: ningún consumidor activo (auditoría de conformidad 2026-07-11, D9 CONFORME). | Infra · Docs | — | docs/superpowers/specs/2026-07-11-retiro-servicios-legacy-design.md · docs/superpowers/plans/2026-07-11-retiro-servicios-legacy.md | — | Implemented — commit **35d9a4d** (346 D + 4 M, review APPROVED 0C/0I con verificación independiente: compose config -q OK con solo los 4 servicios doctrinales + gateway + infra, realm JSON válido sin el cliente y con mappers intactos, git grep residual solo en docs históricos/pointers/detector). **BLOQUE 3 DE COBERTURA CERRADO** — quedan Bloque 4 (equipos-admin), RNF-23 (correo async) y RNF-09 (meta ≥90%). |
| Equipos-admin — ciclo de vida + CRUD admin + historial de nombres + guard BR-E10 (Bloque 4A) | HU-06 (líder elimina su equipo, aun con integrantes; notifica; conserva historial), HU-09 (admin CRUD de equipos: crear/consultar/editar/desactivar/eliminar, sin componer membresía — BR-E05 intacta), HU-48 (participante ve su historial de nombres de equipo), BR-E06 (borrado explícito con limpieza de invitaciones pendientes), BR-E10 (equipo desactivado no se inscribe en partidas nuevas; no se puede eliminar con participación activa en partida Lobby/Iniciada), BR-E11 (historial de nombres sobrevive al borrado). **Identity:** dominio `Equipo` gana `CrearPorAdmin`/`EliminarPorLider`/`EliminarPorAdmin`/`Desactivar`/`Reactivar`/`Renombrar`/`ReasignarLiderazgoPorAdmin`; tabla `historial_nombre_equipo` (con backfill idempotente al arranque, sin migración — Identity usa `EnsureCreatedAsync`); `DELETE /identity/teams/mine` (HU-06) y `GET /identity/teams/mine/history` (HU-48) en `TeamsController`; `AdminTeamsController` (`identity/admin/teams`, policy `AdminOnly`) con 7 endpoints (HU-09); 4 eventos de ciclo de vida (`EquipoEliminado`/`LiderazgoEquipoModificado`/`EquipoDesactivado`/`EquipoReactivado`) + notificador SMTP best-effort (correo a miembros/líderes afectados). **Guard BR-E10 por proyección local de eventos RabbitMQ:** Operaciones emite `InscripcionEquipoCreada`/`InscripcionEquipoCancelada`; Identity estrena su primer consumidor (`OperacionesInscripcionesConsumer`) que mantiene `participaciones_activas_equipo` (dedup por PK compuesta, best-effort ack-siempre). **Decisión de identidad del líder:** el crear-admin recibe el `Usuario.UsuarioId` local y traduce a `Guid.Parse(KeycloakId)` como clave de membresía (el `sub` del JWT == KeycloakId, != UsuarioId). **Clientes:** web página admin "Equipos" (`identidad/equipos`, `adminTeamsApi`) HU-09; mobile pantalla "Eliminar equipo" (con confirmación, HU-06) + "Historial de equipos" (HU-48). **Fuera de alcance:** HU-19 (operador acepta/rechaza inscripciones) = slice 4B en Operaciones, con su propio ciclo. | Identity | Operaciones de Sesión (emite `InscripcionEquipoCreada`/`Cancelada`, único cambio; consume nada) | docs/superpowers/specs/2026-07-08-bloque4a-equipos-admin-design.md · docs/superpowers/specs/2026-07-08-bloque4a-equipos-admin-acceptance.md · docs/superpowers/plans/2026-07-08-bloque4a-equipos-admin.md | contracts/http/identity-api.md · contracts/events/identity-events.md · contracts/events/operaciones-sesion-events.md | Implemented — subagent-driven, 23 tasks (A1..I3), review-clean cada una. Suites verdes en HEAD: Identity **312/312** (224 unit + 47 integration + 41 contract), Operaciones **458** (359 unit + 29 integration + 70 contract), web **84/84** (vitest, build ok), mobile **112/112** (node --test, typecheck ok). **Fuente:** SRS HU-06/09/48 + business-rules BR-E06/E10/E11; hallazgo de la auditoría 2026-07-06 (Bloque 4, sin dueño previo). **Caveat aceptado:** guard BR-E10 eventualmente consistente (proyección por eventos; una inscripción instantes antes del borrado puede no estar proyectada) — elegido sobre HTTP síncrono cross-service. **Minors anotados** (review final): rama sin test en `ReasignarLiderazgoPorAdmin` (NuevoLiderDebeSerDiferente); repos de proyección sin wrap `PersistenceException` (consumidor best-effort lo traga); `isLoadingList` web sin cue visible; fecha ISO cruda en historial mobile. |
| Aprobación de inscripciones por el operador (Bloque 4B) | HU-19 (el operador acepta/rechaza las solicitudes de inscripción pendientes de una partida en Lobby). **Máquina de estados nueva:** toda inscripción (Individual o de equipo) nace `Pendiente` y **solo cuenta** (mínimos, cupo, inicio, convocatorias) tras ser **aceptada** (→`Activa`); rechazar la deja `Rechazada` (terminal y re-solicitable). Enum `EstadoInscripcion { Activa=0, Cancelada=1, Pendiente=2, Rechazada=3 }` (persistido como int, valores nunca renumerados). **Dominio:** `InscripcionPartida.Aceptar(now)`/`Rechazar()`/`OcupaParticipacion`/`EstaPendiente`/`MiembrosSnapshot`; `SesionPartida.AceptarInscripcion(inscripcionId, inscritosActivos, now)` (revalida cupo al aceptar, devuelve las `Convocatoria` a crear en Equipo) y `RechazarInscripcion(inscripcionId, now)`; guards de `Inscribir`/`PreinscribirEquipo` pasan a `OcupaParticipacion` (BR-G09 cuenta Pendiente+Activa) mientras cupo/mínimos siguen sobre `EsActiva`. **Persistencia:** columna `miembrossnapshot uuid[]` (primitive collection sobre backing field) + migración `SP4bAprobacionInscripciones`. **Endpoints operador (`GestionarPartidas`):** `POST .../inscripciones/{inscripcionId}/aceptacion` y `.../rechazo`, ambos 200 + `LobbyDto` (404 sesión/inscripción, 409 cupo lleno / `InscripcionNoPendiente` / no en lobby). **Proyección lobby:** `LobbyDto` gana `solicitudesPendientesIndividual[]`/`solicitudesPendientesEquipo[]`; `mi-sesion` muestra la participación Pendiente (filtro `OcupaParticipacion`). **Eventos:** `InscripcionSolicitada`/`InscripcionAceptada`/`InscripcionRechazada` (forma común de 7 campos) por `ISesionEventsPublisher`, **sin SignalR** (No-Op — el operador consulta el lobby por polling, coherente con SP-3f-2), archivados en el historial de Puntuaciones vía la cola ligada a `operaciones-sesion.#` (sin consumidor nuevo). **Timing de equipo (BR-E10):** `ConvocatoriaCreada` se emite ahora **al aceptar** la preinscripción (no al preinscribir); `InscripcionEquipoCreada` sigue al preinscribir (un equipo Pendiente ya participa, BR-G09); `InscripcionEquipoCancelada` se emite también al **rechazar**. **Clientes:** UI web/móvil **diferida al slice de migración** (documentado en el spec, §5 fuera de alcance). | Operaciones de Sesión | Puntuaciones (archiva los 3 eventos en el historial vía cola `operaciones-sesion.#`); Identity (proyección BR-E10 consume `InscripcionEquipoCancelada` también al rechazar) | docs/superpowers/specs/2026-07-08-bloque4b-inscripciones-aprobacion-design.md · docs/superpowers/plans/2026-07-08-bloque4b-inscripciones-aprobacion.md | contracts/http/operaciones-sesion-api.md · contracts/events/operaciones-sesion-events.md | Implemented — 9 tasks (T1..T9), commit por task. Suite Operaciones verde en HEAD: **487** (386 unit + 31 integration + 70 contract). **Fuente:** SRS HU-19 + business-rules BR-G09 (una participación activa a la vez, ahora Pendiente+Activa) + BR-E10 (timing de eventos de equipo). **Commits:** a668610 (T1 entidad nace Pendiente) · 3cba058 (T2 agregado Aceptar/Rechazar + guards) · eee6363 (T3 snapshot + queries cuentan Pendiente) · c8581b4 (T4 eventos + routing) · 46a1dc0 (T5 handlers Aceptar/Rechazar) · 0db8225 (T6 InscripcionSolicitada + convocatorias diferidas) · 56aa170 (T7 LobbyDto solicitudes pendientes + mi-sesion) · 3da6c1e (T8 endpoints operador) · este commit (T9 contratos de eventos/HTTP + trazabilidad). **Diferido:** cableado clientes web (panel del operador) / móvil (estado Pendiente/Rechazada de la solicitud) → slice de migración. |
| Sincronización incondicional de datos generales con Keycloak (fix HU-02) | HU-02 (editar datos generales de usuario). **Defecto:** al cambiar el correo de un usuario que ya había completado su primer inicio de sesión, el correo quedaba **solo en la BD local** — `UpdateEmailAsync` vivía dentro del `if (mustResendCredentials)`, y ese flag exige credencial temporal pendiente. Como Keycloak es quien autentica, el usuario no podía iniciar sesión con el correo nuevo y sí seguía entrando con el viejo. El mismo acoplamiento dejaba `firstName` desactualizado para siempre (solo se escribía en el alta). Estaba **escrito como criterio aceptado** en el `acceptance.md` de HU-02 («la edición no envía correo ni toca Keycloak»): defecto de especificación, no descuido de código. **Fix:** `IKeycloakIdentityPort.UpdateEmailAsync` → `SyncUserProfileAsync(keycloakId, nombre, correo)`, que hace `PUT` de `username` + `email` + `firstName`; el handler lo invoca **siempre**, sin condicionar por credencial temporal ni por «cambió algo». La incondicionalidad no es un descuido: la BD local ya tiene el dato bueno en los usuarios ya rotos, así que no hay «cambio» que detectar y sin ella el panel no podría reconciliarlos — guardar **es** el camino de reparación. `username` sigue al correo porque Keycloak admite login por `username` **o** `email`: sin eso el correo anterior seguiría siendo credencial válida indefinidamente. `HasTemporaryPasswordAsync` y el reenvío siguen condicionados a que el correo cambie. **Realm:** `editUsernameAllowed: true` es requisito verificado empíricamente (sin él el Admin API rechaza el `PUT` **completo** con `400 error-user-attribute-read-only` — ni el email se aplicaría); `loginWithEmailAllowed: true` se declara explícito por ser la premisa del fix, aunque ya era el default. | Identity · Keycloak (realm) | — | docs/04-sdd/_legacy-implementation-evidence/specs/HU-02-consultar-y-editar-datos-generales-de-usuario/acceptance.md (§Extensión 2026-07-16) | — (sin cambios de contrato: `PATCH /identity/users/{userId}` mantiene body y respuesta) | Implemented — Identity **365 verde** (266 unit + 51 integration + 48 contract). TDD: el test rojo previo al fix devolvía `Collection: []` (Keycloak no recibía nada). Evidencia en vivo contra el Keycloak local: `PUT username` con `editUsernameAllowed=false` → **400** `error-user-attribute-read-only`; con `true` → **204** y `username` aplicado (Keycloak lo normaliza a minúsculas); `username` con espacios/tildes → **400** `error-username-invalid-character` en ambos casos. **Descartado por evidencia:** la hipótesis inicial era que `loginWithEmailAllowed` estuviera en `false`; el realm vivo lo tenía en `true`. **Daño preexistente detectado:** 2 de 3 usuarios reales estaban desincronizados (correo cambiado solo en la BD local) — se reparan guardándolos desde el panel. **Diferido:** el campo `Nombre` es nombre de persona (`nombrePersona`/`TextoHumano`, admite espacios/tildes, no único) y no puede ser el `username` de Keycloak; un campo «nombre de usuario» propio queda como decisión de diseño aparte (~30-40 archivos en 3 bases de código + migración de datos). |
| Mejora de validación de inputs (formato + reglas + feedback en vivo) | Validación consistente de inputs de formulario en las tres capas con backend autoritativo y espejo por campo en los clientes. Regla núcleo **≥1 letra Unicode** (`TextoHumano`) que rechaza `****`/solo-símbolos/solo-espacios en nombres humanos (usuario, equipo, partida, área BDT). Nace del bug "Error de integración con Keycloak" al crear usuario con nombre solo-símbolos (llegaba a Keycloak y explotaba en 502). **Fase A** backend Identity: `TextoHumano` en validators de usuario/equipo + mensaje entendible en `ExceptionHandlingMiddleware` para `KeycloakIntegrationException`. **Fase B** web: `shared/validation.ts` (validadores puros + hook dirty/blur/submit) + `shared/Field.tsx` (error por campo, `aria-invalid`/`aria-describedby`) + parseo de `ValidationProblemDetails` por campo en `identityApi`; cableado crear/editar usuario, crear/renombrar equipo, nombre de partida. **Fase C** mobile: `shared/validation.js` + `createTeamFlow` + feedback en vivo en `CreateTeamScreen`. **Fase D** espacio de error estable (contenedor de una línea siempre presente; `span.field-msg` web / `View minHeight 18` mobile — el layout no salta). **Fase E** login Keycloak (solo correo, sin real-time): `resources/js/umbral-login.js` marca `#username` type=email+required y `#password` required vía script de theme, sin tocar `.ftl`. **Cierre 3 huecos:** backend Partidas gana `TextoHumano` en `NombrePartida` (≤120) y `AreaBusqueda` (sin cota, texto libre); frontend `createPartidaDraft` valida área ≥1 letra; esta fila de trazabilidad. | Identity · Partidas · Frontend (web + mobile) · Keycloak | Gateway (passthrough) | docs/superpowers/specs/2026-07-16-mejora-validacion-inputs-design.md | contracts/http/identity-api.md · contracts/http/partidas-config.md | Implemented — Identity **353** verde (254 unit + 48 controller + 51 integration); Partidas **119** verde (99 unit + 5 integration + 15 contract); web **288** verde; mobile **146** verde. **Fuente:** HU-01/HU-02 (crear/editar usuario) · RB-U0x · BR-E/BR-T/BR-B (equipos/trivia/BDT). **Decisión:** texto de pregunta/opción/QR se queda en `NotEmpty()` (no ≥1 letra) — puede ser legítimamente numérico/simbólico; ≥1 letra solo aplica a nombres humanos y al área de búsqueda descriptiva (BR-B02). **Diferido:** cableado de feedback en vivo del wizard de partidas por campo (hoy valida en bloque al avanzar de paso) → follow-up. |
| Nombres de competidores en pantallas de operador y participante (refinamiento transversal) | Las pantallas de sesión, ranking, geolocalización, pistas e historial muestran el nombre real (`Usuario.Nombre` / `Equipo.NombreEquipo`) en vez del `competidorId` crudo. Endpoint nuevo `POST /identity/directory/names` (policy Default, alcanzable por `Participante`), resolución por sub de Keycloak contra `Usuario.KeycloakId` y por `Equipo.EquipoId`; nombre **actual**, sin consultar el historial BR-E11. Fallback a GUID corto: la resolución nunca rompe la operación | Identity | Web + Mobile (consumidores); Gateway (sin cambios — cae en `/identity/{**catch-all}`, Order 2, Default) | docs/superpowers/specs/2026-07-14-nombres-competidores-design.md · docs/superpowers/plans/2026-07-14-nombres-competidores.md | contracts/http/identity-api.md · contracts/http/gateway-api.md | Implemented — 12 tasks. Suites verdes: Identity 342 (246 unit + 49 integration + 47 contract), Web 258, Mobile 137 + typecheck. **Fuente:** verificación de superficies de operador (ninguna mostraba nombres). Refinamiento transversal sobre HU ya implementadas de lobby, ranking en vivo, consolidado, pistas, geolocalización BDT e historial — **no introduce HU nueva**. **Caveat aceptado:** cualquier usuario autenticado puede resolver el nombre de un GUID que ya conozca (relajación frente a `/identity/users/**`, que sigue `AdminOnly`); justificado en el spec. **Diferido:** nombres de partida y de juego (servicio Partidas)→slice propio. |
| Nombres de partida y de juego en las pantallas (refinamiento transversal) | Las tres superficies que aún mostraban GUID muestran identidad legible: cabecera y columna Juego del historial, celda de partida en rendimiento de equipo, y convocatorias en móvil. `EntradaHistorialDto` gana `juegoOrden`/`tipoJuego` (join con `JuegoProyectado`, indexado en memoria para no hacer N+1); `ConvocatoriaPendienteDto` gana `nombrePartida` (proyectado de `SesionPartida.Nombre`, cambiando la firma del repo a `ConvocatoriaPendienteProyeccion`). La web resuelve nombres de partida con `GET /partidas`, que ya existía | Puntuaciones + Operaciones de Sesión | Web + Mobile (consumidores); Partidas y Gateway **sin cambios** | docs/superpowers/specs/2026-07-15-nombres-partida-juego-design.md · docs/superpowers/plans/2026-07-15-nombres-partida-juego.md | contracts/http/puntuaciones-api.md · contracts/http/operaciones-sesion-api.md | Implemented — 10 tasks, commit por task. Suites verdes en HEAD: Puntuaciones **201** (151 unit + 30 integration + 20 contract), Operaciones **522** (409 unit + 33 integration + 80 contract), Web **273**, Mobile **138** + typecheck. **Fuente:** slice diferido explícitamente en el spec de nombres de competidores (2026-07-14). **Decisión:** `Juego` no tiene nombre en el dominio y no se le añade — la etiqueta se deriva de `Orden` + `TipoJuego` ("Juego 1 · Trivia"); añadir `NombreJuego` sería capacidad de negocio nueva con HU propia. **Hallazgo:** la cabecera del historial no mostraba identidad alguna ("Historial de la partida" a secas), peor que un GUID; entró al alcance. **Fallbacks:** la columna Juego distingue dos vacíos — `—` en eventos de partida (sin juego) y GUID corto cuando hay `juegoId` sin proyección (lag); `useNombresPartida` cae a GUID corto si `GET /partidas` falla, sin romper la pantalla. **Diferido:** `HistorialPartidasScreen`/`RendimientoEquipoScreen` (móvil) y cabeceras de `SesionOperadorPage` — no muestran GUID, así que caen fuera de "sustituir GUIDs visibles". |
| Tiempo real en las pantallas del participante (corrección) | El participante ve la resolución de su solicitud y el arranque de la partida sin pulsar "Recargar". **Causa raíz única:** la pertenencia a grupos de SignalR estaba condicionada a `SuscribirAPartida`, que exige participación previa — pero la aceptación, el rechazo y la convocatoria ocurren **antes** de que esa condición se cumpla (circular por construcción). **Arreglo:** el canal personal `participante:{sub}` se asigna en `OnConnectedAsync` por identidad del JWT, no por partida; `InscripcionAceptada`/`Rechazada` dejan de ser no-op y difunden `InscripcionResuelta` a los destinatarios; el lobby se re-suscribe a `partida:{id}` al ser aceptado (que es lo que habilita el salto automático al iniciar, porque `PartidaIniciada` va al grupo de la partida, no al personal); Convocatorias abre hub por primera vez; lobby y live se re-enganchan al reconectar | Operaciones de Sesión | Mobile (consumidor); Puntuaciones, Partidas, Identity y Gateway **sin cambios** | docs/superpowers/specs/2026-07-15-tiempo-real-participante-design.md · docs/superpowers/plans/2026-07-15-tiempo-real-participante.md | contracts/events/operaciones-sesion-events.md | Implemented — 7 tasks, commit por task. Suites verdes en HEAD: Operaciones **528** (415 unit + 33 integration + 80 contract), Mobile **142** + typecheck. **Fuente:** reporte del usuario sobre el flujo Individual, verificado en código (12 hechos con archivo y línea en el spec §2). **Decisión:** los destinatarios viajan como parámetro del publisher, no dentro del evento — son un asunto de entrega, no un hecho del dominio; así la forma del evento en RabbitMQ no cambia y el `detalle` del historial no se ensucia. **Decisión:** BR-G09 se queda como está — bloquear al miembro por una preinscripción del líder que no hizo y no puede ver lo dejaría retenido a ciegas; cambiarla exige spec propio con visibilidad para el miembro. **Revierte:** el no-broadcast de `InscripcionAceptada`/`Rechazada` declarado en el Bloque 4B, que se decidió mirando solo al operador. **Retirado:** el test `Desuscribir_quita_al_participante_de_su_grupo`, que afirmaba que salir de una partida saca del canal personal — justo lo que este slice deroga. **Hallazgo (no arreglado aquí):** aceptar una convocatoria teniendo participación en otra partida da 409 con mensaje pobre — preexistente. **Diferido:** `PartidasPanelScreen` (el pull-to-refresh es patrón móvil legítimo para un listado de descubrimiento, no un defecto); visibilidad del miembro sobre la preinscripción del líder (capacidad nueva, HU propia). |
| Participación sin puntuar en historial y rankings (corrección) | Quien participó aparece en el historial y en los dos rankings aunque no anotara, con 0 puntos y última posición. **Causa raíz:** el marcador solo nace al acreditar puntos (`ProyectarPuntajeTriviaCommandHandler`) y era el único universo de competidores; en Trivia la pregunta cierra al primer acierto, así que puntúa un solo competidor por pregunta y el resto quedaba invisible. **Arreglo:** proyecciones `participaciones_proyectadas` (desde `InscripcionAceptada`) y `convocatorias_proyectadas` (desde `ConvocatoriaCreada` + `ConvocatoriaRespondida`, unidas por `convocatoriaId`); `RankingCalculator` y `CalculadorRankingConsolidado` operan sobre `participaciones ∪ marcadores`; historial y rendimiento filtran por participación | Puntuaciones | Operaciones de Sesión (emisor de los eventos, **sin cambios**); Gateway, Identity, web y móvil **sin cambios** | docs/superpowers/specs/2026-07-15-participacion-sin-puntuar-design.md · docs/superpowers/plans/2026-07-15-participacion-sin-puntuar.md | contracts/http/puntuaciones-api.md | Implemented — 7 tasks, commit por task. Suite Puntuaciones verde en HEAD: **224** = 170 unit + 34 integration + 20 contract (línea base antes del slice: 201 = 151 + 30 + 20). **Fuente:** pregunta del usuario, verificada en código (13 hechos con archivo y línea en el spec §2). **Hallazgo clave:** el contrato justificaba la limitación con *"no hay evento de inscripción en el broker"* — falso desde el Bloque 4B, que añadió `InscripcionAceptada`. La premisa desapareció y nadie revisó la consecuencia; este slice cobra trabajo ya hecho. **Decisión:** participar = inscripción aceptada (no "haber actuado"); para el miembro, convocatoria aceptada. **Decisión:** proyecciones propias en vez de consultar `EventosHistorial` — hay precedente (`HistorialRepository.cs:52-58`), pero el caso del miembro exigiría un join sobre JSON (`convocatoriaId` vive en `DetalleJson`) y el historial está para narrar, no para rankear. **Decisión:** sin backfill — las partidas anteriores conservan el comportamiento previo. **Retira:** la limitación documentada *"el integrante que jamás autoró una acción de juego no ve la partida"*. **Corrección sobre el plan (T5/T6):** el plan sustituía la fuente del historial y del rendimiento por participación **sola**; sin backfill eso habría vaciado el historial de las partidas anteriores, violando el criterio de aceptación 8 del propio spec. Las dos consultas van por `participación ∪ marcador`, coherente con el principio que el spec ya aplica en los calculadores; hay test de no-regresión por caso (`Partida_anterior_al_slice_*`). **Fuera del plan (T3):** `ObtenerMarcadorQueryHandler` era un tercer llamador del calculador que ni spec ni plan listaban; recibe las participaciones para que el inscrito que no anotó vea su 0 en vez de un 404, coherente con aparecer ya en el ranking del juego. **Deuda menor:** `IHistorialRepository.GetEquiposDelParticipanteAsync` queda sin consumidor de producción (solo lo cubre su test de repositorio); la interfaz sigue viva para otros handlers. **Verificación de no-regresión:** los contract tests siguen verdes **sin modificar** (`git diff --name-only HEAD~6 -- .../ContractTests/` vacío) — cambia quién sale en las listas, no la forma de los DTOs. |
| Nombre de partida en el historial del participante (móvil) | El participante no podía saber **qué partida** miraba en su propio historial: `HistorialPartidasScreen` encabezaba con la modalidad (`"Individual · 120 pts"`) y `RendimientoEquipoScreen` con la posición; ninguna mostraba GUID — simplemente no había identidad. **Arreglo:** endpoint de directorio `POST /operaciones-sesion/directory/partidas` (lote de hasta 200 ids → nombres, desde el snapshot `SesionPartida.Nombre`) + hook móvil `useNombresPartida` con caché y troceo; el nombre pasa a ser el título de la tarjeta y modalidad/puntos/posición/fecha bajan a la línea de contexto | Operaciones de Sesión | **Ninguno.** Puntuaciones, Partidas, Gateway, Identity y web **sin tocar** (verificado con `git diff --stat`) | docs/superpowers/specs/2026-07-15-nombre-partida-historial-movil-design.md · docs/superpowers/plans/2026-07-15-nombre-partida-historial-movil.md | contracts/http/operaciones-sesion-api.md | Implemented — 10 tasks. Suite Operaciones verde: **546** = 423 unit + 37 integration + 86 contract (línea base antes del slice: 528 = 415 + 33 + 80). Móvil: **163** tests + `tsc --noEmit` limpio (línea base: 142). **Fuente:** pregunta del usuario ("¿en el historial aparece el nombre de la partida?"), verificada en código — no aparecía en ninguna capa: ni en el DTO (`HistorialPartidasResponse`), ni en la proyección (`PartidaProyectada`), ni en el evento (`PartidaPublicadaEnLobby`). **HU gobernante:** HU-27 (implementada en SP-4d); toca incidentalmente la superficie de HU-49 y la cabecera de sesión en vivo — **no introduce HU nueva**, igual que los dos slices hermanos de nombres. **Hallazgo clave:** el slice hermano `2026-07-15-nombres-partida-juego` dejó estas dos pantallas fuera de alcance por escrito ("añadirles identidad de partida es una mejora distinta") y descartó un directorio **en Partidas** por dos razones —exigiría anular la policy `OperadorOAdministrador` del catch-all del gateway, y no resolvería la columna Juego—; **ninguna aplica en Operaciones**: `/operaciones-sesion/**` ya es `Default` y la columna Juego no está en alcance. El rechazo era al sitio, no al patrón. **Decisión:** descartada la denormalización del nombre en `PartidaPublicadaEnLobby` → `PartidaProyectada` (misma razón que el hermano: dejaría sin nombre a las partidas ya proyectadas salvo backfill, que son justo las que el participante ya jugó); el directorio **no necesita backfill** porque existe fila `SesionPartida` para toda partida publicada y el historial solo contiene `Terminada`. **Decisión:** el web **no migra** al resolver nuevo — sigue con `GET /partidas` (`useNombresPartida.ts`); la asimetría web/móvil la impone el gateway y es deliberada. **Fuera del plan:** arreglado el literal hardcodeado `nombre: "Mi partida"` de `PartidasPanelScreenContainer.tsx:25-26`, que el participante veía en la cabecera al **retomar** una sesión activa (una partida `Iniciada` no está en `partidas-publicadas`, así que su nombre solo puede venir del directorio); se resuelve al cargar el panel, no en el `onPress`, para no colgar el tap. **Correcciones del diseño halladas al implementar (T3):** el controller es despachador puro y **no** inyecta `IValidator<T>` como el de Identity —Operaciones valida en el `ValidationBehavior` del pipeline (doctrine audit M-2)— y su 400 es `{ message }`, **no** `ValidationProblemDetails`; el request record va en `Application/DTOs/`, no en `Api/Contracts/` (carpeta inexistente aquí; la regla graded pide `DTOs/`). **Limitación declarada (T4):** el integration test **no** prueba la traducción a SQL —todo el suite de persistencia de Operaciones usa InMemory y el único `UseNpgsql` (`ConcurrencyTokenTests.cs:13-15`) solo construye el modelo—, así que la traducción Npgsql de `partidaIds.Contains(...)` queda sin verificar, igual que la de todos los métodos del repositorio: hueco **sistémico preexistente**, no introducido aquí. **Limitación declarada (móvil):** las tres pantallas son `.tsx` y `node --test` no puede importarlas; por eso la lógica con casos borde se extrajo a módulos puros (`useNombresPartida.js`, `historialLabels.js`) en vez de quedar dentro del render — a diferencia del hermano `useNombres.js`, cuya caché y degradación quedan sin cubrir. **Verificación:** mutación deliberada del handler tumba 2 unit + 2 contract, y quitar el marcado de ids no resueltos tumba su test de caché — los tests muerden. **Regresión cero:** ningún test existente modificado (las tres superficies no aseveraban identidad de partida porque no la mostraban). |
| Orden del listado de partidas del operador | El operador ve sus partidas ordenadas por fecha de creación descendente (la última creada primero), con esa fecha visible en la tabla | Partidas | — (ningún otro servicio participa) | docs/superpowers/specs/2026-07-16-orden-listado-partidas-operador-design.md · docs/superpowers/plans/2026-07-16-orden-listado-partidas-operador.md | contracts/http/partidas-config.md | Implemented — backend 117/117 verde (96 unit + 7 integration + 14 contract), línea base 112 (94+5+13): +2 unit, +2 integration, +1 contract. Frontend 275/275 verde (36 archivos) + `tsc --noEmit` limpio. **Causa raíz doble:** `ListAsync` no tenía `ORDER BY` —Postgres devolvía el orden físico, que además cambia al actualizar una fila— y el modelo no tenía campo por el que ordenar. **Decisiones:** la fecha entra al dominio como parámetro (último, por paridad con Operaciones: `Inscribir(..., fecha)`); el `ORDER BY` vive en el repositorio, no en el handler; `ThenBy(PartidaId)` desempata porque un reloj falso en tests devuelve el mismo instante y sin él el orden sería intermitente; `fechaCreacion` se expone solo en el listado, no en `GET /partidas/{id}`. **Verificación:** mutación deliberada —quitar el `OrderByDescending`— tumba el test de orden (Task 3 Step 5); el contract test asevera `NotEqual(default, FechaCreacion)` y no la mera forma, que pasaría sin mapeo. **Limitación declarada:** el `ORDER BY` se prueba sobre **InMemory** (ordenamiento LINQ en memoria, no SQL real): todo el suite de persistencia de Partidas usa `UseInMemoryDatabase` y el único `UseNpgsql` (`ConcurrencyTokenTests.cs:13-15`) solo construye el modelo sin conectar — hueco **sistémico preexistente**, no introducido aquí. **Limitación declarada:** el `defaultValueSql` del centinela **no lo prueba ningún test** (ninguna migración lo está); verificado a mano contra Postgres local con `\d partidas` (Task 2 Step 7) — es su única verificación. **Nota de test-craft:** el test de la columna compara `textContent` en vez de usar `toHaveTextContent`; el string de `toLocaleString()` en es-VE trae un espacio duro (U+00A0) y el matcher normaliza el texto del elemento pero no el esperado, así que falla mostrando un diff de cadenas visualmente idénticas. **Regresión cero:** ningún assertion existente modificado; en `PartidasListPage.test.tsx` solo cambian las dos fixtures, por el tipo y no por el comportamiento. Corrige además el comentario stale de `Partida.cs` que decía que SP-3 escribe el `Estado`: ADR-0010 lo movió a Operaciones y aquí no se escribe nunca. |
| Tipado del id local y renombrado del subject | El id local del usuario y el sub de OIDC dejan de ser dos Guid confundibles: el local es un tipo propio y el estado de equipos se llama SubjectId | Identity | — (ningún otro servicio participa) | docs/superpowers/specs/2026-07-16-id-competidor-tipado-design.md · docs/superpowers/plans/2026-07-16-id-competidor-tipado.md | contracts/http/identity-api.md | Implemented — 350/350 verde (252 unit + 51 integration + 47 contract), línea base 345 (249+49+47): +3 unit (UsuarioLocalId), +2 integration (mapeo EF de UsuarioLocalId). **Causa raíz atacada:** el bug `dfc56c7` compiló porque `Usuario.UsuarioId` (local) y `ParticipanteEquipo.UsuarioId` (sub de OIDC) eran ambos `Guid` con el mismo nombre. Ahora el local es `UsuarioLocalId` (`readonly record struct` + `ValueConverter` de EF, patrón `PartidaId`) y el estado de equipos se llama `SubjectId`. **Verificación decisiva:** reintroducir el bug —filtrar `usuario.UsuarioId` a un `HashSet<Guid>` de miembros— ahora es error de compilación `CS1503` (Task 2 Step 12), no un fallo silencioso de runtime. Los tres tests de regresión de `dfc56c7` siguen verdes sin tocar sus aserciones. **Limitación declarada:** el tipado es **fricción, no garantía** — nadie impide un `.Valor` distraído que vuelva a filtrar el id local; sube el costo de equivocarse y hace visible el cruce, no lo hace imposible. **Limitación declarada:** el `ValueConverter` se asevera sobre el **modelo de EF**, no por round-trip: todo el suite de persistencia usa `UseInMemoryDatabase`, que no ejercita converters, así que un round-trip pasaría sin el converter registrado. **Limitación declarada:** el renombrado de columnas **no lo cubre ningún test** (Identity no tiene migraciones EF); el parche ALTER se verificó a mano contra Postgres con `\d`, con los datos intactos (3+3 filas) y comprobando idempotencia en segunda corrida. **Fuera de alcance declarado:** parámetros locales y nombres de métodos de repositorio que reciben el sub siguen genéricos (engañan igual pero no son estado y siguen compilando); y el lock-in del sub en tres bases, que necesita ADR propio. |
| Auto-aceptar la convocatoria del líder (corrección de regla) | Un equipo de solo el líder no podía arrancar nunca: el mínimo en Equipo cuenta inscripciones activas **con ≥1 convocatoria aceptada** (`sp3e1` §5), el líder es integrante y por tanto recibe convocatoria **a sí mismo**, y hasta aceptarla su equipo contaba 0 → `AplicarInicio` cancelaba la sesión entera. Afecta HU-15 (unir equipo), HU-16 (convocatoria), HU-20 (inicio) | Operaciones de Sesión | — | docs/superpowers/specs/2026-07-16-autoaceptar-convocatoria-lider-design.md · docs/superpowers/plans/2026-07-16-autoaceptar-convocatoria-lider.md | — (sin cambios de contrato) | Implemented — **553/553 verde** (429 unit + 38 integration + 86 contract; +7 nuevos, los 2 clave vistos en rojo antes del fix con el síntoma exacto del usuario: `Expected: Iniciada / Actual: Cancelada`). **No era un defecto de implementación**: el código cumplía `sp3e1` §5 al pie de la letra; la regla aprobada era la equivocada. La convocatoria del líder nace `Aceptada` (`InscripcionPartida.LiderId` nuevo + columna `liderid`, migración `20260716191533`, filas existentes → `Guid.Empty` = sin auto-aceptado retroactivo). **Guardado por BR-G09**: el handler calcula `liderPuedeAutoAceptar` con `ParticipanteTieneParticipacionActivaAsync`; si el líder ya participa en otra partida su convocatoria queda `Pendiente`, igual que en el camino manual. El flag es **fail-closed por diseño** (default `false` = no auto-aceptar): omitirlo cancela la partida (ruidoso) en vez de violar BR-G09 (silencioso), y por eso los 95 llamadores legacy de `AceptarInscripcion` compilan sin tocar sus aserciones. El auto-aceptado emite también `ConvocatoriaRespondida` porque `ConvocatoriaCreadaEvent` no lleva `Estado`. El mínimo **no se toca**: sigue exigiendo ≥1 aceptada por equipo. Migración verificada contra la BD real (`liderid uuid NOT NULL DEFAULT Guid.Empty`, 21 filas preexistentes con el default). **Pendiente de verificar por el usuario:** flujo HTTP completo. **Fuera de alcance:** el operador inicia a ciegas — el lobby no le dice cuántos equipos cuentan para el mínimo, así que un equipo multi-miembro seguirá cancelándose si inicia antes de que alguien acepte (ventanas observadas de 5–38 s). Merece su propio slice. |
| QR generado para la etapa BDT (HU-28 · RF-25 · BR-B03 refinada) | El operador ya no teclea el código del tesoro de cada `EtapaBDT`: pulsa "Generar QR", el frontend crea un UUID y renderiza el QR imprimible, y puede descargarlo o regenerarlo antes de guardar. El dominio exige que el código sea un UUID (`EtapaBDT.Crear`) y único dentro del juego, case-insensitive (`JuegoBDT.Crear` → `ValidarCodigosUnicos`), y lo guarda **literal** (sin normalizar casing/formato) | Partidas | — (ningún otro servicio participa; la validación automática del tesoro en Operaciones de Sesión —comparación exacta de texto contra `CodigoQREsperado`— no cambia una línea) | docs/superpowers/specs/2026-07-16-qr-generado-etapa-bdt-design.md · docs/superpowers/plans/2026-07-16-qr-generado-etapa-bdt.md | contracts/http/partidas-config.md | Implemented — backend **126/126 verde** (105 unit + 7 integration + 14 contract), línea base 117 (96+7+14): +9 unit en `JuegoBDTTests` (5 casos de código no-UUID rechazado + 1 de código literal sin normalizar + 2 de códigos duplicados/case-insensitive rechazados + 1 de códigos distintos aceptados). Frontend **286/286 verde** (37 archivos) + `vite build` limpio, línea base 275 (36 archivos): +11 (`qrTesoro.test.ts` nuevo con 5 tests del módulo de generación/render; `CreatePartidaPage.test.tsx` +4 —genera y descarga, falla la generación, borrar etapa no arrastra el error, falla la regeneración—; `PartidaDetailPage.test.tsx` +2 —muestra y descarga el QR, no ofrece regenerar—). **Fuente:** HU-28 (RF-25) ya implementada — cambio de regla y UX, no introduce HU nueva. **Decisión clave (D1):** el frontend genera y el backend valida, no al revés — Partidas no admite editar un juego ya creado, así que un código generado en el servidor sería inmutable desde su nacimiento y el operador no podría verlo ni regenerarlo antes de guardar. **Decisión clave (D2, guardar literal):** la comparación en Operaciones es exacta contra el texto embebido en el QR físico; normalizar el código en el backend habría hecho que ningún tesoro validara jamás. **Decisión clave (D5):** la regla de UUID vive solo en el dominio, no se duplica en `AgregarJuegoBDTCommandValidator` (que sigue comprobando únicamente `NotEmpty`), para no repetir la misma regla en dos sitios libres de divergir. **Momentos en que se ve el QR:** al generarlo (creación, con descarga) y al reimprimir (detalle de partida, con descarga); **nunca** en sesión activa (`Iniciada`) ni al participante — el gateway ya bloquea `/partidas/**` a `OperadorOAdministrador` y el panel BDT en vivo no se toca. El detalle **no** ofrece regenerar: no habría dónde escribir un código nuevo sin un endpoint de edición que Partidas no tiene. **Cero cambios** en Operaciones de Sesión, Puntuaciones, Identity, gateway y móvil. **Pendiente de verificar por el usuario:** Step 4 del plan — flujo real end-to-end (crear partida BDT, generar/regenerar/descargar QR, guardar, reimprimir desde el detalle, y decodificar el PNG descargado con un lector de QR para confirmar que el texto coincide exactamente con `codigoQREsperado`) — no ejercitado en esta verificación porque requiere infraestructura, Keycloak, gateway y frontend levantados. |
